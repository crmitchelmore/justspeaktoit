# #919 qualification plan: establish an Aurora contract before integration

Decision: NARROW per reviews/919.json. Produce a bounded, dated provider qualification report now. Add an adapter only after the actual supported contract, permitted account access and real-audio evidence exist. An announcement, health response, model name or saved credential is insufficient. No selectable placeholder, speculative OpenAI compatibility, vendor outreach, paid activation or discovery-automation restart is authorized.

## Current evidence and concrete deliverable

Create Docs/aurora-stt-qualification.md on a documentation branch, recording source URLs, check date, observed result, missing requirement and next owner action. This is the SOL-medium deliverable even if access stays blocked; it is not an integration PR and does not close #919. Retain the 8 September issue evidence as historical, distinct from these 12 September 2026 read-only planning checks:

| Primary surface | Observed result / limit |
| --- | --- |
| https://bluemachines.ai/ and https://bluemachines.ai/platform | Public enterprise platform pages. Targeted vendor-domain searches and inspected page content yielded no Aurora inference request/response contract. JavaScript page shells limit static inspection; absence here is not proof no private API exists. |
| https://blog.bluemachines.ai/ | HTTP 200 public blog; the inspected current index did not expose Aurora API documentation. |
| https://api.bluemachines.ai/ | Direct unauthenticated GET returned HTTP 200 with `{"status":"ok"}`. This is health only. |
| https://console.bluemachines.ai/ | Direct GET returned an HTML application shell, HTTP 200. No authenticated account or developer documentation was inspected; do not repeat the older authentication-redirect result as newly verified. |
| https://huggingface.co/blue-machines | Public model metadata inspection did not establish Aurora inference access or an STT model contract. Do not download weights or infer API availability from unrelated model entries. |

Vendor [privacy policy](https://bluemachines.ai/privacy-policy) and [terms](https://bluemachines.ai/terms) are general platform documents; their existence does not establish a specific Aurora retention, training, pricing or service-region promise. Record provider-specific unanswered questions without inventing guarantees or giving legal clearance. An enterprise voice demo or another provider named in a Blue Machines tutorial is not an Aurora API.

The exact current blocker is missing authoritative endpoint/model identifier, authentication/entitlement, supported request/audio and response/error schemas, transport lifecycle, limits and permitted inference access. List these individually as unavailable rather than claiming a vendor rejection or technical incompatibility.

## Finite follow-up pass

Use the above official surfaces and links they publish; perform one bounded primary-source pass, at most 30 minutes, before finalizing the dossier. Search vendor-owned pages for Aurora and developer/API documentation, following documented links only. Do not enumerate speculative endpoints, upload test audio to guessed URLs, reverse-engineer private console routes, or treat frontend demo credentials as public integration authorization. If static rendering blocks a linked public documentation page, an ordinary read-only browser check may clarify it; a login wall remains an access gate.

Record each of these fields with a source or “not established”: canonical model ID/version; file versus live transport; endpoint and authentication format; account entitlement and existing free quota; audio encoding/sample-rate/channel/container limits; supported languages and language-hint syntax; maximum duration/size/concurrency; synchronous result versus job polling; segment/timestamp/confidence semantics; errors/rate limits; cancellation and finalisation; pricing and data handling. If only batch is documented, live remains unsupported. If the service requires account setup, sales enablement or payment, record the exact gate and stop without contacting anyone or enabling billing.

A complete public contract can still leave account access pending. Conversely, an authenticated console health result without a contract cannot justify an adapter. Report these states separately. No repeated background checking is part of this issue plan.

## Conditional real-audio qualification

Only after authoritative contract and authorized existing free access are available, prepare the smallest supported synthetic spoken fixture, approximately 5–10 seconds, with an exact non-sensitive reference sentence, first/last words and known encoding. Use a local existing fixture or authorized generation; no personal recordings, new paid synthesis or purchases. Record fixture checksum/duration/format, request model and non-secret parameter shape, request/start/final timing, HTTP or protocol status, parsed transcript and sanitized errors. Never persist keys, signed request URLs, headers or account details in the repository.

First prove one actual transcription and a bounded failure/cancellation path appropriate to the published transport. For live support, require ordered audio acceptance and explicit stop/drain/final result, including first/last words; receipt of an interim is not finalisation. Do not run deliberate quota-exhaustion experiments. A small success demonstrates access and that fixture only; multilingual accuracy, advertised financial-domain quality and production latency need separate representative measurements. A language claim must cite actual documented coverage and, where product behavior relies on it, an appropriate consented fixture.

## Integration boundary after qualification

Update this issue’s implementation section with the verified contract before code work. Reuse Sources/SpeakCore/TranscriptionProviderRegistry.swift’s TranscriptionProvider methods (transcribeFile, validateAPIKey, requiresAPIKey, supportedModels) and metadata; keep existing exported signatures. Place one shared concrete client in SpeakCore and a thin Mac provider adapter only for the supported transport. Use #1114’s multipart seam only if the actual body/response contract matches; use existing job lifecycle patterns if asynchronous. Do not make a second generic provider framework.

Register a real model through ModelCatalog.swift/ModelCatalogTypes.swift and capability projections, then existing SpeakApp/TranscriptionProviderRegistry.swift and SpeakiOS/Services/IOSTranscriptionSession.swift routes or their landed #1113 equivalents. Coordinate #1116 credential catalogue; adding one credential does not authorize automatic CloudKit/key-transfer policy expansion. Preserve explicit model/location selection and #999’s strict-offline rules. Use friendly History names without exposing provider response bodies or credentials in user-facing errors; follow #996’s safe mapping where applicable.

Add client contract fixtures for authenticated request shape, malformed/error responses, cancellation and actual result mapping. Extend ProviderCatalogInvariantTests, BatchTranscriptionProviderRoutingTests, ModelCatalogTests and iOS BatchProviderRoutingTests as applicable; if live is verified, use the existing StreamingTranscriptionClient/result-snapshot/boundary architecture and actual start/send/finish fixtures. Native macOS and iOS builds, required lint/tests/API checks and supported-platform picker/settings/History smoke are mandatory before integration acceptance. Hardware/provider microphone gates remain distinct from Linux fixtures. A real-audio call plus mocks still does not prove all device behavior.

Finish the current report with a clear outcome: contract unavailable, contract available/access unavailable, or qualified for a specifically supported transport. Identify the missing evidence and owner, keep #919 open until actual integration is validated and merged, and let the normal automatic release follow its own verification. No manual release dispatch, Stable promotion or revived daily discovery task is implied.
