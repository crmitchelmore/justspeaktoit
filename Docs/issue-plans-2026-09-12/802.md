# #802 executable plan: launched batch post-processing and same-process recovery

Decision: NARROW to the first missing production-orchestration slice in reviews/802.json. Extend the merged Carbon→synthetic recording→strict HTTP→clipboard→durable History journey; do not rebuild the harness or claim the full P0 epic complete. No live provider requests/credits, real microphone requirement, streaming matrix or AX-permission bypass. Existing #707/#800/#801 captured-target/clipboard contracts remain unchanged.

## Concrete increment

Add two native scenarios: batch post-processing-on success, and batch post-processing failure followed by a fresh successful capture in the SAME Speak process. The recovery scenario proves another recording works without stale state; it need not add the separate Retry-previous-recording journey. Both begin with real supported Control–Option–Shift–K Carbon events while the existing target fixture owns focus, exactly as CoreJourneyBatchUITests currently does. Never call MainManager start/stop/process or assign clipboard/History results directly from a test seam.

### Extend only the existing boundaries

1. `Sources/SpeakApp/CoreJourneyLaunchProfile.swift`: retain UUID-isolated defaults, file manager, temporary keychain namespace, guarded directory and DEBUG-only activation. Add a closed scenario enum, e.g. batchOff/batchPolishSuccess/batchPolishFailureThenSuccess, from one explicit fixture environment option validated only inside a valid core-journey profile. Unknown requested scenarios fail closed. Existing SPEAK_CORE_JOURNEY_BATCH=1 behavior stays off-scenario compatible. Apply ordinary settings before managers subscribe: postProcessingEnabled=true, a fixed synthetic post-processing model/prompt, postProcessingStreamingEnabled=false, and existing clipboardOnly/restore=false. This intentionally tests nonstreaming post-processing; SSE/streaming remains a separate gap. Enable the production HUD for the two new scenarios so failure/retry guidance can be observed, without creating fake test UI or changing release HUD defaults.
2. `Sources/SpeakApp/CoreJourneyBatchFixture.swift`: extend its isolated URLProtocol and diagnostics to distinguish strict batch-audio and text-post-processing requests. Continue intercepting EVERY request in the fixture session, denying unknown hosts/methods/auth/model/body and never forwarding to network. Batch must contain exact existing WAV bytes and expected batch model. Post-processing must contain the actual expected system/user-message structure generated from the raw transcript via TranscriptCleanupPolicy, selected synthetic model and stream=false; do not accept any arbitrary text-shaped request. Use a fixed prompt and deterministic response. Keep fixture key synthetic. The existing OpenRouterAPIClient is already shared by TranscriptionManager and PostProcessingManager in WireUp; reuse it so production serialization, key-availability checks and routing execute.
3. Give the multi-run scenario deterministic distinct raw/processed outputs A/B and a synchronized request sequence owned by the fixture instance/profile. First batch returns RawA, its expected post-processing request returns a controlled nonretryable HTTP error with synthetic safe body; second batch returns RawB, its post-processing returns ProcessedB. Expected failure is an ACCEPTED fixture request with a failure response, not an unexpected/rejected request. Assert exact sequence and counts; an extra automatic retry, unexpected third run, wrong model/raw text or stale A prompt in the B request fails. Do not use arbitrary sleeps or test-written command files to bypass the real orchestration transition. Keep diagnostics atomic and scenario-scoped; no mutable response handler shared between concurrently launched profiles.
4. The production semantics in `MainManager.stopSession` already continue raw delivery on post-processing failure, record a post-processing HistoryError, retain retry data/hint and end in failed state after delivery/History append. Success stores original raw plus processed text and delivers processed output. Pin those behaviors without changing the pipeline. If the test exposes an actual mismatch, retain exact reproduction and propose the smallest separately reviewed fix; do not change expectations merely to make CI green. `MainManager+PostProcessing.friendlyPostProcessingMessage` supplies the current user-facing failure; avoid widening #996 into macOS taxonomy work here.

### Native test assertions

Add `Tests/SpeakAppUITests/CoreJourneyPostProcessingUITests.swift` as an explicitly selected/required suite. Share only useful launch/gesture/diagnostic helpers from CoreJourneyBatchUITests; preserve its existing off case and strict assertions. Keep profile cleanup and pre-test clipboard restoration, plus diagnostics/screenshots on failure.

Success: assert same captured PID, fixture retains focus, ordered real Carbon keyDown/keyUp/doubleTap/singleTap evidence, production idle→recording→processing→delivering→completed, exactly one batch and one post-processing request, zero unexpected requests, exact processed clipboard text and one durably flushed History row. History must retain RawA plus ProcessedA, expected batch/post-processing model usages, duration, trigger/output and saved audio semantics. Post-processing is not proven by a final string alone: confirm actual HTTP receives raw text and History retains both forms.

Failure/recovery: first run ends failed with RawA on clipboard, RawA stored once in durable History, nil processed text, expected post-processing error and visible production “Post-processing failed” plus retry guidance. A diagnostic failureMessage alone is insufficient proof of visible feedback; inspect the real HUD/accessibility surface. Verify the fixture field using the ACTUAL event-posting permission branch: when granted expect exactly one raw paste, otherwise unchanged target and an explicit clipboard-only coverage note. Preserve error evidence before the next run clears it.

Start/stop a second recording through Carbon without app relaunch, fixture relaunch or clearing History; assert the PID remains unchanged. Observe fresh state/gesture events by checkpoint offsets rather than confusing first-run completion with second-run completion. Second run delivers only ProcessedB, creates exactly one additional distinct History identity, preserves first-run raw/error row unchanged, records RawB/ProcessedB and no stale failure. Final field contains each expected delivery exactly once when posting is allowed, or remains unchanged when unavailable; clipboard contains ProcessedB without stale A/raw replacement. Review the same production History/delivery events used by the existing test to reject duplicate output/history, rather than asserting unreliable cross-process clipboard changeCount increments.

The existing15.2-second stop-gesture wait is an intentional consequence of XCUI/Carbon timing. Reuse it; do not shorten the production gesture or replace Carbon with a direct callback to make tests faster. Record actual native duration before adding further matrix cases. Fixed synthetic250ms WAV is not real speech/first-word evidence.

## CI, artifacts and qualification

Update `.github/workflows/ci.yml`'s core-journey-fixture-ui selection with the new suite and `scripts/verify-core-journey-ui.py` required manifest. Keep all existing selected suites and fail-closed missing/failed/skipped behavior. Strengthen verification for the two named new test cases if suite-level success could hide one missing scenario; use the existing parser and bounded required-case checks rather than a second verifier. Extend `scripts/tests/test_core_journey_gate.py` for absent new suite/case, skip and failure. If helper/fixture unit tests form a new required component suite, add it to the existing run-core-journey manifest with matching verification; do not remove other15 suites to fit time.

Use `CoreJourneyLaunchProfileTests`/focused fixture tests for default-off/backward-compatible scenario decoding, invalid profile isolation, exact request admission and expected response ordering. Confirm Release code cannot activate the fixture. Preserve xcresult/log/coverage artifacts and existing atomic diagnostic/History attachments, including failure runs; all artifact text must be synthetic. Do not disable posting/AX errors or claim a skipped assertion as editor success.

Run portable verifier tests, then exact-head Apple native component and UI gates plus normal Debug/Release build/lint as applicable. Retain the ten-minute end-to-end CI budget including setup/launch; report measured job duration and per-scenario time. Do not increase timeout or convert failures to skips to meet the goal. If this bounded slice exceeds budget, first use evidence to reduce redundant setup while preserving same-process recovery and authentic gestures.

Update `Docs/core-journey-e2e.md` with the two newly executed journeys, precise permission-dependent coverage and remaining P0 gaps. The epic still requires20 green runs including five cold runners before claiming flake qualification; record actual runs over the authorised CI workflow, not20 repeated samples on one warm runner or retries that hide failures. Physical microphone/Fn, direct AX insertion, streaming, broader targets and native permissions remain separate gates (#611/#664). Merged tests/clipboard success do not close those gaps or the epic. No release dispatch or provider billing is authorised.
