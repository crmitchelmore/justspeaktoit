# Issue #270 — fix(ios): Apple live transcription clears text after speech pause

## Stance: APPROVED
**Date:** 2026-04-08

## Summary
Bug fix in `Sources/SpeakiOS/Services/iOSLiveTranscriber.swift`. Extends the existing `committedText`/`commitIfImplicitReset`/`restartRecognitionTask` pattern to cover edge cases where Apple's SFSpeechRecognizer drops accumulated text on silence timeout.

## Security assessment
- **Trust boundary**: unchanged — on-device only, no new network flows
- **Permissions**: no new permissions needed (mic + speech recognition already declared)
- **Data handling**: `committedText` stays in memory; cleared on `stop()`/reset (line 172)
- **Logging**: existing `print` statements log char counts only, not content — implementation must maintain this invariant
- **Sentry**: no captures in this file

## Approval rationale
Entirely local state management. No credentials, no new attack surface, no API surface changes. The single non-blocking caution: any new log statements added by the fix must log error codes/char counts, not transcription content.

## Cross-role notes
- Morgan (Architecture) flagged error-callback path not committing — security-neutral but implementation should not add content logging there
- Jordan (Reliability) noted blast radius is iOS-only — consistent with security view
- Quality and Performance are still pending
