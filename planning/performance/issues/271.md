# Issue #271 — fix(web): burger menu breaks UI layout on iOS/mobile viewport

**Stance:** Approved
**Date:** 2026-04-08

## Summary
Pure `landing-page/index.html` CSS/JS fix. Burger menu toggle is `classList.toggle('open')` — O(1). No new assets. No app code involved.

## Performance assessment
- Entirely off the app hot path (audio, transcription, HUD)
- Static HTML/CSS — no build pipeline, no framework overhead
- No page weight increase expected
- No layout thrash risk beyond pre-existing

## Blockers
None.

## Approved because
Fix is bounded to cold-path marketing page CSS. No performance concern.
