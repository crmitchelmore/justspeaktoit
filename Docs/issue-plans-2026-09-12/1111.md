# #1111: one OpenAI transport, staged platform adoption

NARROW/IMPLEMENT with SOL medium. Read AGENTS.md, review 1111, and landed #1104/#1105 contracts. Do not delete public iOS adapters or force universal routing to meet line-count criteria. Implement in two reviewable stages: shared transport consumed by existing platform owners, then Mac/shared-capture routing only after parity tests. iOS may retain its public capture adapter. Parent integrates; Beads is absent.

## Scope and evidence

`OpenAIRealtimeWebSocketClient` on iOS has accepted-prefix overflow ownership, readiness flush and ordered admission worth preserving. `OpenAIRealtimeLiveTranscriber` owns recorder, audio-session/interruption observers, startup observations, recording-loss tracking and tail drain. Mac `OpenAIRealtimeLiveController` owns item-ID composition/segments/keyterm hints. None is equivalent to forwarding raw delta/final strings.

The existing `Sources/SpeakCore/OpenAITranscriptionModels.swift` already owns GA session payload/model-specific prompt and language rules. Reuse it unchanged: endpoint `wss://api.openai.com/v1/realtime?intent=transcription`, Bearer, no OpenAI-Beta header, session.type transcription, 24 kHz PCM, near_field and turn_detection null. Current official guide confirms append/commit, item-scoped completions and unordered completion arrival across turns. [OpenAI realtime transcription, checked 12 September 2026](https://developers.openai.com/api/docs/guides/realtime-transcription). Do not add current guide's new keyword/delay features or change selected models.

## Stage 1: shared implementation and thin platform consumers

Create `Sources/SpeakCore/OpenAIRealtimeLiveClient.swift` and a focused internal parser/assembler file if needed. Public client conforms to FinalizingStreamingTranscriptionClient and the single #1105 snapshot capability. `finalShape = .cumulativeTranscript`; all ordinary callbacks carry composed session text, never raw deltas incorrectly labelled standalone finals. `finishFlushesBufferedAudio = true`.

Provide the issue's public initializer (`apiKey:model:language:prompt:sampleRate:session:`), with sane existing defaults and explicit session injection. Internally reuse/move the iOS `OpenAIRealtimeSocket` seam; reconcile with #1104's socket seam rather than inventing a broad transport framework. Keep the original public `SpeakiOSLib.OpenAIRealtimeError` enum and `OpenAIRealtimeLiveTranscriber` symbols through adapters; moving an enum's module and replacing it with a typealias does not preserve its old binary symbol. Core may define its own error and the iOS adapter maps cases, especially preReadyAudioOverflow, to the existing public error type. This avoids unnecessary breaking releases.

State ownership: one session identity covers socket, send completions, parser updates, readiness waiters, overflow admission and every deadline. Starting again aborts/resolves old state before reset. Never call callbacks/resume continuations under state locks. Ordered submission serialises session.update, accepted prefix, live PCM and commit; send accounting is per session. Retain the **first** five seconds of 24 kHz PCM16 (240,000 bytes), reject the crossing chunk, report overflow exactly once outside locks, and refuse all later audio even after session.updated. Do not replace this with suffix eviction. The accepted prefix can still finish; the owning platform decides save/deliver versus discard. Overflow is not automatic abort/isTerminal true because that would prevent prefix finalisation.

Readiness requires session.updated (legacy transcription_session.updated also accepted), not session.created or task.running. Flush the full accepted prefix into send accounting before resolving readiness waiters; new PCM cannot overtake it. Bound readiness and send completion separately (existing readiness 1 second, send stage maximum 1.5 seconds), retaining terminal send-budget failure behaviour. Never copy the iOS unbounded DispatchGroup wait. Track/clear pending timers; no fixed startup sleep.

Item assembler: map by item_id; append deltas within each item; completed replaces that item's deltas/final, not all history. Keep first-observed item order as existing fallback; use committed item's ordering metadata only after exact current wire fields are verified. Preserve missing-ID fallback `_pending`, repeated text on distinct item IDs, empty completion clearing its item and late correction to an older item without erasing another item's pending text. Emit no artificial utterance boundaries (existing owners don't). Do not dedupe by text.

Finish: atomically arm waiters/snapshot pre-stop completed IDs; stop admitting new capture audio; drain accepted PCM/tail; await config/prefix readiness and audio send completion; apply explicit Mac grace; send one input_audio_buffer.commit; bound its completion; then await a matching new committed item's completion or model budget. Capture any `input_audio_buffer.committed` item identity when supported; a stale prior completion cannot resolve stop. If no reliable commit identity is available, retain all responses through the bounded budget instead of claiming first-new-item is guaranteed acknowledgment. Completion may arrive synchronously before commit-send completion, so store it against armed state and process after send ordering is satisfied. Return whole composed session text with outstanding deltas on timeout, preserving existing platform fallback; no trailing final also emitted through callback. Empty session returns nil and should not hang waiting for a nonexistent transcript. Concurrent finish joins; immediate stop aborts/resumes all callers once. Keep total within existing owner deadlines; derive model budgets from canonical capabilities, with iOS's existing 0.5-second policy explicitly supplied by its adapter.

Add `LiveClientOptions.openAITranscriptionPrompt: String?` using compatible forwarding initializers. Mac adapter builds it with the exact current assemblyAIKeyterms trim/comma-join helper; do not forward postProcessingSystemPrompt. iOS supplies nil. Factory .openai returns the shared client; .apple remains nil. #1104's initializer/factory work stays intact.

For snapshots, use confirmed text/zero-time final item segments and an explicit ordered display projection. Multiple pending items can be interleaved with finals; confirmedText plus one pending suffix cannot represent that. Extend the existing snapshot struct with `displayText: String? = nil` (preserve its prior initializer via forwarding overload if already exported); generic controller prefers nonnil displayText without treating pending text as confirmed. Other providers retain nil/fallback. No new protocol. Snapshot confirmedText remains genuinely confirmed; metadata unsupported by OpenAI stays nil.

Adapt `Sources/SpeakiOS/Services/OpenAIRealtimeLiveTranscriber.swift` to feed shared client/sendAudio and await finishAndWait; retain its public API, audioRecorder, captureID/recordingLoss, interruption/configuration observers, safety ownership and onStartupObservation. Replace private item dictionaries with composed callback/full-return state only after comparison fixtures pass. Preserve queue barrier then converter tail then finish. Its result remains same modelIdentifier/empty iOS segments unless separately intended. Thin compatibility transport wrapper can remain temporarily for internal tests; remove duplicated socket/parsing implementation once consumers switch.

Likewise adapt Mac `OpenAIRealtimeLiveController` first to use core client/snapshot, preserving capture/model/prompt/input cleanup. A thin internal adapter can bridge callbacks during this step; there must be only one active provider transport/parser. Do not route iOS IOSTranscriptionSession to generic capture yet merely because factory now exists.

## Stage 2: gated Mac fold and optional iOS route adoption

After Stage 1 native/ownership tests, Mac may move openai/ to sharedClient and remove its controller plus Mac live transcriber/parser/factory only after reference search. Preserve any batch provider/validation consumers and error definitions still used. Ensure #1105 generic snapshot/boundary and options handling cover all Mac behaviour before deletion.

iOS generic-capture migration is conditional follow-up within this issue, not mandatory deletion. Keep `.openAI` backend and public adapter if generic SharedClientLiveTranscriber has not demonstrated equivalent loss/overflow, recorder and interruption ownership. A factory invariant must explicitly allow that adapter. If later migrated, preserve public class as forwarding façade and port IOSTranscriptionSession+SafetyRecording and startup/backend diagnostics deliberately. Do not change observable public fields/types or delete symbols to satisfy “no OpenAIRealtime files” acceptance.

## Tests and gates

Create `Tests/SpeakCoreTests/OpenAIRealtimeLiveClientTests.swift`: real session payload, model/prompt/language fields, readiness aliases, delta accumulation/completed replacement/item ordering, identical distinct items, empty events, stale completion, synchronous completion race, all finish stages/stalls, accepted-prefix overflow/replay, restart and cancellation.

Port eight parser tests from `OpenAIRealtimeProviderTests` (session created/updated, delta/empty delta, completed, error, unknown, garbage) to the core parser; retain five catalogue/capabilities tests. Retarget its three model-name tests to `OpenAITranscriptionModels.apiModelName` or equivalent existing tests, naming replacements in PR. Port all eight `OpenAIRealtimeOverflowTests` against core seam, preserving exact accepted-prefix/one-error/race assertions. Keep `OpenAIOverflowOwnerTests` on iOS: foreground/history, hardware destination, keyboard original-owner, stop-near-overflow and cancel-new-run behaviours belong to platform ownership, not a core socket test.

Retain and run TranscriberStartupCancellationTests, LiveCaptureInterruptionTests, RecordingLossOwnerLifecycleTests and StartupDiagnosticsObservationTests through the retained adapter. Add full-session contract/snapshot/factory checks. No test is deleted merely because its type moved.

Apple Swift required: focused suites, make lint/build/test, API compatibility against PR base and existing ci.yml iOS tests/build. Linux checks prove only diff/reference/fixture review. AGENTS requires a real OpenAI WebSocket probe before shipping protocol changes; with existing authorised key verify GA ack, model/prompt and immediate first/last words on Mac and iPhone, short/silent recording, overflow recovery and restart. Do not purchase credits or infer microphone success from simulation. Stage PRs may proceed with explicit native/provider gates outstanding; no release dispatch or unnecessary public API break.
