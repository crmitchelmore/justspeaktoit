# Issue #246 — Security Stance

**Status:** No action (MCP auth gap)

All GitHub MCP reads returned empty arrays (get, get_comments, get_labels). Consistent with documented MCP auth gap pattern on this private repo.

**Action taken:** noop — wait for re-trigger when issue content is accessible.
