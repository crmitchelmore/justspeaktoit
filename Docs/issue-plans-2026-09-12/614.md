# #614 qualification plan: Apple live engines and hands-free capture

Decision: NARROW to native/device evidence and corrected acceptance wording. No new analyzer/VAD implementation. Older comments claiming hands-free is absent are stale: current MainManager+HandsFree and IOSHandsFreeDictationCoordinator implement default-off arming, SpeechDetector input, preroll, silence stop, cancellation and visible state. Reconcile #658's current review as its follow-up owner; do not create parallel work. Credit existing AppleLocalModels defaults and progressive analyzer implementation.

## Benchmark existing engines

Use existing builds on physical OS26 iPhone and Mac, plus an older supported OS/device for fallback. Record exact source/build, OS, hardware, microphone route, locale, prepared assets, selected model and actually active engine. An analyzer ID selected in Settings does not prove analyzer execution when fallback occurs. AppleLocalModels prefers supported SpeechTranscriber, then DictationTranscriber, then legacy; do not infer SpeechTranscriber capability from OS version alone or require Apple Intelligence for DictationTranscriber.

On each supported device compare20 paired repetitions of the same synthetic30-second passage under the chosen analyzer and legacy Apple Speech, alternating order. Record cold preparation separately from already prepared/warm runs. Reuse existing latency/session data for first-partial and, where the insertion destination exists, first-insert distributions with sample counts. Verify first/final words and complete final text separately from timing. Compare analyzer first-partial p50 against legacy as the issue's requested no-worse target, report p95 and missing/failed samples, and avoid declaring a performance win from selection/default code. iOS result display is not macOS AX insertion. No press WER/speed claim or “no model download” promise is evidence; explicitly prepare required assets through current UI before warm comparison.

## Hands-free device matrix

Armed VAD necessarily monitors microphone input and buffers preroll. Replace the ambiguous “no capture while silent” acceptance wording with “no dictated transcript or unintended remote transcription while silently armed”; never suggest the microphone is off in armed mode.

| Journey | Evidence to record |
|---|---|
| Default-off installation and explicit arm | Hands-free remains off until chosen; armed indicator and microphone permission/state are truthful |
| Silent arming, then speech | Silence creates no History transcript/provider transcription session; speaking activates one capture with first words preserved |
| Onset timing | Measure audible speech onset to observed capture activation/first accepted speech; report checkpoint and sample count against approximately300ms target, separately from first-partial latency |
| Configured silence hold | Capture stops/finalises once after actual configured hold; final words and destination survive; detector rearms visibly |
| Immediate next utterance | Preroll and first words survive the transition without joining unrelated captures |
| Manual stop/disarm/cancel during start | No late fallback capture or hot microphone after teardown; explicit disarm stays disarmed |
| Interruption/route change | Observe finalisation/retained result and actual rearm policy; committed results survive a failed detector restart |
| Unsupported OS/device/locale or missing assets | Correct availability and actionable preparation/fallback behavior; cancellation does not activate legacy recognition |
| Remote model selected, silence only | No remote transcription connection/audio transmission caused solely by arming; verify through existing diagnostics/network evidence before claiming it |

Use built-in mic and one available Bluetooth route. Record meaningful environmental noise/false activations rather than silently excluding them. Coordinate the first-word and interruption rows with #935/#947; if #999 strict-local changes are included in the tested build, verify their local-only contract and record the build distinction. Do not retroactively claim old fallback paths were strictly local.

Run relevant existing HandsFreeVoiceActivity, hands-free settings/lifecycle, AppleModelStartup, ownership and finalisation tests on Apple CI as supporting logic evidence. Tests and Linux source inspection cannot establish microphone onset timing, SpeechAnalyzer quality or physical teardown. Retain evidence in the existing #614/#658 owner records; parent handles authorised publication. Raise only observed, narrowly reproduced gaps for separate review. Do not close this parent until both comparative measurements and hands-free device rows are supported; unavailable devices leave explicit pending gates. No release, telemetry activation or new benchmark framework is authorised.
