# Issue #149 — Agentic workflow live retest after secret refresh

**Status:** product-approved (confirmed after second correction, 2026-03-25T03:19Z)
**Last updated:** 2026-03-25T03:19Z

## Summary
Maintainer disproved Contents API as a signed-commit-safe path (verified: false, unsigned). Three valid portability paths remain: (1) workflow commit signing, (2) explicit planning/* branch exemption, (3) dedicated repo for scale. Product approved because all three paths preserve persistent role memory value.

## Key facts verified
- Contents API writes: `verified: false, reason: unsigned` — disqualified for signed-commit environments
- Docs/agentic-workflows.md documents exemption and signing paths explicitly
- planning/* branches work in this repo today (no enforcement)
- All three remaining options (workflow signing, branch exemption, dedicated repo) preserve memory persistence and therefore the product value

## Product stance
- Product value is unchanged: persistent role memory improves cross-issue planning quality
- All three corrected paths preserve that value — choice between them is an Architecture/Security decision
- Maintainer explicitly asked Product to confirm or change approval; approval is unchanged
- Ephemeral memory remains acceptable last-resort only if all three paths are unavailable, documented explicitly

## Cross-role dependencies
- Architecture: must recommend the portable default from the three valid paths
- Security: must say whether workflow signing is preferred default, with branch exemption as fallback

## Approval status
Product-approved. Waiting on Architecture and Security to settle the canonical path recommendation.
