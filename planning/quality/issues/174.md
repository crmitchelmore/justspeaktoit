# Issue #174 — Port final career-framework agentic workflow pattern

## Summary
Workflow/CI-only change. Brings justspeaktoit to parity with career-framework's agentic planning pattern. No Swift code changes. Changes: workflow triggers, agent persona files, doc updates.

## Current stance: needs-quality (first pass)

### Open concerns
1. **Verification story absent**: Success criteria list 5 outcomes but no smoke-test protocol for new trigger chains (e.g., test issue, fork test, staging branch).
2. **Redundant layer removal is unspecified**: `issue-triage` and `product-validation-dispatch` overlap exists (both fire on `issues: opened`). The "if in place" qualifier is right but the plan needs to say which files get deleted/disabled, and whether it's atomic.
3. **persona.md source**: Named personas (Alex Hale et al.) need a stated source — copy from career-framework or defined fresh here?

### Repo facts verified
- Both `issue-planning-kickoff.lock.yml` and `issue-triage.lock.yml` currently trigger on `issues: [opened, reopened]` — overlap is real.
- No PR plan-review workflows exist yet in `.github/workflows/`.
- Agent files live in `.github/agents/` (6 files: agentic-workflows, 5 planning roles).
- No `persona.md` files currently exist in `.github/agents/`.
