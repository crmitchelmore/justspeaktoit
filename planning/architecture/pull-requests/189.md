# PR #189 — fix(mac): prevent Data.subdata crash from non-zero startIndex

**Status:** needs-architecture (first review)
**Branch:** fix/data-subdata-crash
**Changed files:** Sources/SpeakApp/TranscriptionManager.swift only

## Summary
4-line surgical fix. Replaces `pendingPCMData.removeFirst(offset)` with
`pendingPCMData = Data(pendingPCMData.dropFirst(offset))` in four places:
- `AssemblyAILiveController` (audio tap loop + flush path)
- `ModulateLiveController` (audio tap loop + flush path)

## Architecture stance
- No module boundary changes. Single file, single module (SpeakApp).
- No new seams, no new abstractions, no coupling changes.
- Fix is semantically correct and applied symmetrically to both providers.
- Only blocker: no linked planning issue (`Plan issue:` or `Closes #` absent).
- This is likely a direct crash-fix PR not expected to go through full planning.

## Open questions
- Does this codebase require a planning issue for crash-fix PRs? Protocol says yes, but this may warrant a lightweight exemption note.

## Resolution needed
Either link a planning issue or the maintainer should clarify that crash fixes bypass the planning lane.
