# PR/Issue #283 — fix(ios): add missing SpeakCore import

## Note
#283 is an **issue**, not a PR. The PR plan-review workflow was triggered by an `issue_comment` on this issue. Per protocol, no action taken: comment belongs to an issue, not a pull request.

## Issue context (for reference)
- **Title**: fix(ios): add missing SpeakCore import — unblocks TestFlight releases
- **State**: open
- **Labels**: `planning:in-discussion`, `planning:needs-architecture`, `planning:design-approved`, `planning:reliability-approved`, `planning:needs-product`, `planning:needs-security`, `planning:needs-performance`, `planning:needs-quality`
- **Triggered**: comment ID 4214986075

## Observation
This is a single-file import fix (`import SpeakCore` in `SpeakiOSApp.swift`). It crosses module boundary correctly — `SpeakCore` is already a dependency of the iOS app target, just not imported where needed. No architectural concern from Morgan's lens.
