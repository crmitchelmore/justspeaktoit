# Issue #201 — perf(mac): replace lowercased+distance with caseInsensitive search

**Status:** No planning labels — no action taken (protocol: no planning labels, no kickoff comment)

## Summary
Automation (Daily Perf Improver) proposes replacing `text.lowercased()` + two `O(n) distance(from:to:)` calls in `TranscriptionTextProcessor.expandClipboardCommands` with `result.range(of:options:.caseInsensitive)`.

## Verified facts
- Code exists exactly as described in `Sources/SpeakApp/TranscriptionTextProcessor.swift` lines 59-82
- Change is correct and safe: `.caseInsensitive` handles ASCII triggers identically; returns range directly into result string
- Hot path claim is valid: function fires on every transcription result when voice commands enabled
- No planning labels present; get_comments returned empty (integrity filtered)
- Branch: `perf-assist/text-processor-casefold-search-a8f0b69de56d595b`

## Performance assessment (if planning were opened)
- Miss path (typical): removes 1 heap alloc + 1 O(n) scan on lowercased copy
- Hit path: removes 1 alloc + 2×O(n) distance walks
- No measurable benchmark but code-level case is airtight; no synthetic baseline needed
- Would approve without further questions
