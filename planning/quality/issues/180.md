# Issue #180 — Ignore closed-PR comments in plan-review workflows

## Latest stance: APPROVED

## Summary
Workflow guard tightening only. Add `github.event.issue.state == 'open'` to the `issue_comment` path in all five PR role workflow files and the PR bot follow-up dispatcher.

## Verified repo facts
- Five role workflow files affected: product, performance, quality, security, architecture (all `.lock.yml`)
- Each has the guard at **two** locations: main job `if:` (line 65) and `pre_activation` job `if:` (line ~1062)
- `pr-plan-review-bot-follow-up.yml` has a single `if:` at line 19 with the same pattern
- Total: 11 guard occurrences across 6 files

## Approval rationale
- Scope is surgical and bounded (trigger/guard only, no approval model change)
- Acceptance criteria map directly to inspectable YAML
- No app code or test changes needed
- Pattern: workflow-only guards with clear before/after verifiable from the diff

## Non-blocking guardrail
Implementer must update both guard locations in each `.lock.yml` (not just the first one). Line 65 and the `pre_activation` job guard at ~line 1062.
