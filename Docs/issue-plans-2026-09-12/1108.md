# #1108: fold ElevenLabs after realtime parity

IMPLEMENT with SOL medium on an isolated branch, parent integration. Read repository guidance, review 1108 and the actual landed #1104/#1105 changes. Their completion is blocking: current main's shared ElevenLabs client uses the wrong `/stream` protocol. #1104 owns correcting it; this issue must not implement a competing socket/finish system. #1106's boundary/segment pattern is useful but its Cartesia close command is irrelevant here. Beads is unavailable.

## Scope and behaviour

| Dedicated Mac behaviour | Shared requirement |
|---|---|
| Hard-coded `scribe_v2_realtime`, xi-api-key, realtime URL, JSON/base64 PCM | Already corrected/tested by #1104; factory route must resolve same model |
| Waits `session_started`; five-second pre-start cap | Preserve bounded ordered replay and short-recording startup finish |
| 100 ms PCM frames; final residual padded to **100 ms** | Move framing into shared client; do not inherit Cartesia/AssemblyAI's 50 ms minimum |
| Converter drain, send drain, user grace, commit and 1.5 s wait | Add grace to existing #1104 bounded finish, keeping its safer full drain |
| Standalone committed segments, repeated utterances retained | Segment snapshot via #1105; never treat identical text as duplicate |
| Last interim survives in final Mac result | Preserve through upstream result snapshot, without changing #1104's confirmed finish return |
| Dedicated callback only updates partial; no utterance boundary | Use upstream optional boundary suppression; no new polish trigger |

Evidence: `Sources/SpeakApp/ElevenLabsLiveController.start/handleTranscript/stop/buildFinalResult`, nested `ElevenLabsAudioProcessor`, `ElevenLabsLiveTranscriber.sendCommit/awaitCommitFinal/parseResponse`. The old Mac parser accepts both committed and timestamp-enriched events as separate finals, and its first-commit waiter can finish before a delayed tail. Those defects must not be copied back over #1104's fixes.

## 1. Verify prerequisite transport; add framing/settings only where absent

Files: `Sources/SpeakCore/ElevenLabsLiveClient.swift`, `LiveTranscriptionClientFactory.swift`, existing `LiveClientOptions.swift`, and `Tests/SpeakCoreTests/ElevenLabsLiveClientTests.swift`.

Inspect #1104 tests and production path before changes. It must use `/v1/speech-to-text/realtime`, `audio_format=pcm_<rate>`, `commit_strategy=vad`, xi-api-key, JSON input_audio_chunk with base64 PCM, handshake-gated preroll, and empty commit:true on finish. It must keep all delayed committed segments through its 1.5-second post-commit window/terminal close and ignore timestamp enrichment when disabled. These protocol facts were checked in #1104 against the [official realtime reference](https://elevenlabs.io/docs/api-reference/speech-to-text/v-1-speech-to-text-realtime). No additional research is needed unless the landed behaviour conflicts with these fixtures or provider acceptance fails.

Preserve every existing public initializer, both sendAudio forms, isConnected meaning, validator, error enum, factory overload and finalisation symbol. Add a required `LiveClientOptions`/upstream timing overload and retain exact old signatures as forwarding shims. Factory passes the existing macOS settings projection's `stopGracePeriod`; no provider settings branch in shared controller and no AppSettings access in SpeakCore. Shared routing supplies language to the already-supported shared client, whereas old Mac ignored it; document this deliberate convergence and test default plus locale-normalised configured language without adding new language options.

If #1104 does not already frame arbitrary PCM, add a small internal framer before JSON encoding: 100 ms preferred and final nonempty minimum (3200 bytes at 16 kHz), derived from sample rate. Never emit silence for an empty session. Residual plus converter tail is padded once, then encoded before commit; preserve byte order, alignment and cap. Reuse upstream internal framing helper only if its durations are configurable; do not create a public audio framework or add pacing sleeps. Ensure both public audio entry points reach the same framer and old APIs do not bypass it.

Integrate user grace into #1104's session-scoped finish task. Preserve the old grace-before-commit behaviour, keep receiving during grace, and arm finalisation state before commit can synchronously produce a response. Do not let grace authorize more capture audio after finishing begins. Bound readiness/send work as upstream already does; define its existing four-second overall safety ceiling **excluding valid user grace**, then account for grace explicitly so a real configured value is not silently eaten by that deadline. Verify the resulting maximum remains below the Mac manager's ten-second timeout. On transport failure/abort return promptly rather than sleeping a cosmetic grace period.

Keep `finishFlushesBufferedAudio = true`, multiple-waiter safety, session generation guards and graceful stop-before-handshake replay. Use #1104's fake transport/deadlines, not a second test-only implementation. A stalled send must not allow commit to overtake unsent PCM; failure is bounded and retains available text.

## 2. Preserve result segments and trailing interim without false finals

Files: `ElevenLabsLiveClient.swift`, `StreamingTranscriptionClient.swift`, `Sources/SpeakApp/SharedClientLiveController.swift` and upstream shared-controller tests.

Conform to #1105 `StreamingTranscriptSnapshotProviding`; capture one zero-time segment per actual committed_transcript, including finals consumed during finish. Clear snapshot at new start, retain until controller reads it after finish, and never add timestamp enrichment twice. Confidence, cost and raw payload remain nil, with controller duration/model preserved.

A direct route switch would drop valid Mac fallback text: #1104 deliberately returns confirmed text only, while the dedicated Mac result appends outstanding interim. Preserve both contracts using #1105's single `StreamingTranscriptSnapshotProviding` capability, not another pending-interim protocol. The snapshot supplies explicit confirmedText (including empty string for interim-only cases), pendingInterim and segments. It tracks latest replacement interim and clears it when its committed event is accepted; later interim after an earlier commit remains pending. Do not derive pending text by comparing prefixes. Unsupported metadata stays nil.

At Mac result creation the #1105 snapshot projection uses confirmed text plus pending interim once, independent of whether the confirmed finish return was nil. Never append that interim onto latestTranscript, which may already contain it. Update displayed text as unconfirmed when appropriate; it is not a fabricated committed segment. Other conforming clients provide their own exact confirmed/interim separation; nonconformers use legacy defaults. iOS continues its existing confirmed-return handling. No rich-update, pending-interim or metadata-specific protocol is added here.

Use #1105's optional boundary conformance and #1106's no-emission pattern to suppress generic final/full-history boundary fallback for ElevenLabs. The dedicated Mac controller never forced polishing at provider commits. Enabling new standalone boundary callbacks is optional later work. Do not add ElevenLabs checks to generic controller methods.

## 3. Route and delete only redundant live code

After native focused tests pass, route `elevenlabs/` to `controllers.sharedClient` in `SwitchingLiveTranscriber.controllerRoutes`. Remove `ControllerSet.elevenLabs` (verify actual casing), its construction and all entry. Delete both `Sources/SpeakApp/ElevenLabsLiveController.swift` and `Sources/SpeakApp/ElevenLabsLiveTranscriber.swift`.

Search all references to `ElevenLabsLiveTranscriber`, `ElevenLabsTranscriberError` and deleted controller before removal; if an error is genuinely used by retained code, move that small definition instead of deleting the consumer. Keep `ElevenLabsTranscriptionProvider.swift`, shared `ElevenLabsAPIKeyValidator`, batch APIs, settings migration, TTS and credential invalidation behaviour unchanged.

The issue's four named Mac test files do **not** currently reference `ElevenLabsLiveTranscriber`. Keep all tests in `ElevenLabsTranscriptionProviderTests.swift`, `ElevenLabsClientValidationTests.swift`, `ElevenLabsModelMigrationTests.swift` and `ElevenLabsKeyRemovalTests.swift`; none should be deleted as transport-only. If controller constructor wiring affects the key-removal fixture, adapt its setup while retaining assertions. PR deletion map should explicitly say no direct Mac transport test existed and cite #1104's replacement protocol coverage.

## Focused verification

Extend existing core/controller suites rather than duplicating #1104 tests:

- Framing: several small chunks, exact 100 ms, large chunk split, final 1–99 ms padding, converter tail before empty commit, empty stream no padded PCM; byte round-trip through JSON/base64.
- Settings/route: exact legacy initializer/factory calls compile; correct realtime API model, configured language, zero/custom/max-valid grace. A held audio send blocks commit; grace precedes commit; late VAD plus later manual-tail commits both survive.
- Snapshot: two finals, repeated identical finals, timestamp-enrichment ignore, segments include stop finals, latest interim replaces, final clears interim, timeout with confirmed-plus-interim and interim-only preserve Mac result without duplicated suffix or fake final segment.
- Controller: final returned text replaces once, no boundary callbacks, old callback/deadline after restart cannot affect result, failed-start and preferred-input cleanup remain intact. Include route/lifecycle and key-invalidation assertions.
- Preserve #1104 handshake/startup cap, concurrent finish/abort, error/timeout and all contract fixtures; no need to rerun unrelated provider research.

Linux checks are whitespace/reference/static evidence only. Apple Swift/Xcode gates: focused suites, `make lint`, `make build`, `make test`, `scripts/check-api-compatibility.sh <PR-base-SHA>`, and existing ci.yml iOS build/tests. Real Mac with existing authorised credentials: immediate first word, stop on last word, very short/silent speech, multiple utterances, custom grace, cancel/restart and preferred mic release; verify partial cadence and final History/output contain first/last words exactly once. Shared tap is 4096 versus old 1024, so measure first-partial latency. Real iOS smoke checks shared framing/finish. Record actual results before closure; do not invent native/provider success, purchase credits, bypass PR gates or publish releases.
