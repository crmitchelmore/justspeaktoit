# #664 executable plan: two-app insertion runbook and remaining device results

Decision: NARROW to a concise documentation/result template and remaining physical AX qualification. No insertion rewrite, allowlist expansion, promotion of the experimental setting or release dispatch. #611 owns wider-app expansion and latency benchmarking; #664 qualifies the current TextEdit/Notes allowlist plus one-shot baselines. Existing default-off/allowlist and reconciler tests are supporting evidence, not physical app results.

## Resolve the evidence conflict explicitly

Use Chris's detailed [13August run](https://github.com/crmitchelmore/justspeaktoit/issues/664#issuecomment-5286541336) as primary historical evidence: signed/notarised mac-v2.49.0, build202608131906, source c1c747528ce7964f75d8aa02b96adf7d303a7387. That report records TextEdit append, selected-text replacement, before/after edits and focus change as exact-match passes; inside-region editing passes preservation safety while delivery is reported failed. Notes, experimental-setting-off and Sublime Text one-shot baselines are BLOCKED because the Mac locked. Preserve its reported OS/app strings verbatim as historical observations; do not normalize odd version labels into new facts.

The later [bravostation summary](https://github.com/crmitchelmore/justspeaktoit/issues/664#issuecomment-5379274639) incorrectly says both allowlisted apps passed. Clearly note this discrepancy in the runbook's historical status. Do not edit old comments or close the issue from that summary. New results require an unlocked interactive session and cannot be supplied by this Linux workspace.

## Documentation-only implementation

No dedicated insertion runbook was found under Docs; `Docs/sre-review-2026-09-05.md` only links this pending qualification. Add `Docs/streaming-insertion-verification.md` with:

- Present scope/current bundle allowlist from `Sources/SpeakApp/StreamingInsertionAllowlist.swift`: com.apple.TextEdit and com.apple.Notes. Other apps remain one-shot. Link #611 for expansion; no suggested “temporarily allowlist Slack” step.
- The corrected historical evidence table above, separating exact transcript equality and safe preservation. Keep each historical pass bound to its actual build/scenario; it is not a blanket current-build pass.
- Fixed configuration: signed direct-distribution build, unlocked Mac, Accessibility granted, Remote or Local Streaming with named model, Smart or Accessibility output, Insert at Cursor, experimental toggle state and explicit post-processing on/off. Replace Field is outside qualification. Use existing authorised credentials and synthetic text; save and restore changed settings after testing.
- An empty reusable result table with columns: source/build, macOS, app/version, named field, scenario, toggle/post-process, observed pause/fallback, exact final text match, unrelated-text preservation, History delivery/firstInsertMs, result, evidence link. Include blocked/pending/not-applicable reasons rather than empty inferred passes.

Link this runbook from the existing SRE-review reference only if appropriate; do not rewrite its dated findings as new observations. No application code/tests changes are required to create the runbook. Check relative links/headings and compare terminology against actual code.

## Remaining physical run

First compare current tested insertion/reconciler/output code against the historical source. Retain TextEdit historical rows and rerun only materially affected scenarios; record what changed and why a rerun is needed. Notes and both baselines remain mandatory new observations even if source is unchanged.

In a named Notes editor field test visible partials, genuine correction/retraction, selection replacement, emoji/diacritics, pre-existing duplicate phrases, edits before/inside/after region, and focus change to another clean field/app. Verify final transcript equality against the appropriate final History text and preserve surrounding text separately. Do not compare the whole pre-populated field to a transcript without accounting for its expected prefix/suffix. Test post-processing disabled first to match historical configuration; add a bounded enabled run to cover the final-polish delta if the current contract still promises polished delivery. A provider run without an observed revision does not qualify correction/retraction.

Run setting-off in an otherwise supported field, then a named Sublime Text field or documented equivalent nonallowlisted app: no partial insertion and exactly one final delivery. Record actual field state, not just HUD success. Ensure History reports the observed method and a meaningful firstInsertMs; that timestamp is not independent screen-paint evidence.

`LiveTextInserter.streamingFinalize(with:)` deliberately returns `.applied` in some lost-target/final-patch failure paths after partials landed to avoid duplicate insertion. Those outcomes can pass preservation while failing exact final delivery. `.unknown` region yields failure; `.absent` can defer to one-shot. The template must record these separately and must never use `.applied`/“Delivered” alone as exact-match evidence. A safe inside-region user edit is not permission to overwrite that edit merely to achieve equality.

If a currently supported field fails, retain exact reproduction/build and refer a narrowly scoped fix or disablement for separate review before experimental promotion. Do not expand this task into a value-independent AX strategy. Any later Swift fix requires relevant LiveTextInserterStreamingTests, StreamingInsertionSettingsTests and native Apple build/lint, plus rerun of affected physical rows. Documentation may complete independently; #664 cannot close while Notes/baselines or required exactness evidence remain blocked. Parent owns authorised result publication; no release or telemetry is implied.
