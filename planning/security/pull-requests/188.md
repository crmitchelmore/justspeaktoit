# PR #188 — feat(agentic-workflows): add Riley Tan (Design/UX) as 8th planning team member

**Date:** 2026-03-26
**Branch:** chore/add-designer-role
**Status:** Open — BLOCKED (missing plan issue linkage)

## What the PR does
- Adds Riley Tan as 8th planning team member (Design/UX reviewer)
- New `planning-design.agent.md` persona file
- New `issue-planning-design.lock.yml` and `pr-plan-review-design.lock.yml` compiled workflows
- New `issue-planning-design.md` and `pr-plan-review-design.md` source files
- Updates all 7 existing agent personas with cross-role dynamics for Design
- Updates kickoff, reconciler, ready-check, synthesis for 7 issue approvals (was 6)
- Updates PR plan review for 6 PR approvals (was 5)
- `plan-review:needs-design` and `plan-review:design-approved` labels referenced
- Note: incorporates the AgenTek improvements from PR #186 (which was closed without merge)

## Security observations
- **No `Plan issue:` reference in PR body or commit message.** Primary blocker per established protocol.
- Permissions pattern consistent with established pattern: `permissions: {}` top-level.
- New design workflow job permissions: `contents: read, issues: read, pull-requests: read` — correct least-privilege.
- New PR design workflow job permissions: `contents: read, issues: read, pull-requests: read` for agent job, write for conclusion job — consistent.
- `COPILOT_GITHUB_TOKEN` secret used (same pattern as other workflows).
- Fork guard preserved in lock.yml for PR-triggered workflows.
- Agent persona files scoped to `planning/*` branches — low risk.
- No `pull_request_target` usage.

## Blocker
Missing `Plan issue: #<n>` in PR body. Protocol requires a linked planning issue at `planning:ready-for-dev` before security approval.

## Cross-role context
This PR follows PR #186 which was also blocked on missing plan issue and was closed without merge. Same pattern.
