name: Publish speak CLI (retired)
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'CLI assets are frozen with release-train candidates. Prepare and approve a new Stable candidate to change them.'
          exit 1
