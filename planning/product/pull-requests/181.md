# PR #181 — fix(agentic-workflows): ignore closed PR comments

**Plan issue:** #180 (`planning:ready-for-dev`, all 5 roles approved)
**Status:** Product-approved

## Alignment check
- Issue #180 scoped: add `github.event.issue.state == 'open'` guard to PR role review workflows and dispatcher; keep `pull_request`/`workflow_dispatch` unchanged.
- PR delivers exactly that. Three-point verification: `gh aw compile`, lock files confirmed, failure traced to closed PR #161.
- No scope drift. No user-facing product impact.

## Open cross-role items
Four other roles (`needs-security`, `needs-performance`, `needs-quality`, `needs-architecture`) still pending.

## Product decision
Approved 2026-03-25. Tooling reliability fix with zero product surface impact.
