# #1121 execution plan: qualify existing sherpa usage evidence

Decision: NARROW, read-only evidence only. No runtime implementation is authorised by this issue review. Assign SOL medium only if further evidence assembly is needed; the parent already completed the bounded schema qualification. Read reviews/1121.json and `posthog-1121-evidence.md`. No new telemetry, remote post, model removal, native binary addition, App Store guard change or owner branch choice.

## Established evidence and honest result

The parent's authenticated read-only inspection on 12 September 2026 verified Just Speak To It organization `01a02667-88df-0000-4772-ee67677b0954`, production PostHog project254700, UTC: [production project](https://eu.posthog.com/project/254700/). The discovered observed custom events are app_active_daily, analytics_opt_in, first_transcription_succeeded and onboarding_completed. transcription_completed is absent from that returned schema. The observed first_transcription_succeeded properties include engine_type/provider_type but no model_family, selected-model or runtime identifier; returned engine_type values contain cloud only.

This is schema/coverage evidence, not a 90-day runtime-usage query. No query result or distinct-user count exists, and neither absence of an event nor cloud-only first-success values means zero local usage. First-success is activation evidence rather than repeated runtime use. Opt-in/offline behaviour and historical schema gaps limit extrapolation to installs. The evidence brief must state requested 90-day sherpa usage is unavailable from the discovered qualified source. Do not issue a speculative query with missing fields, turn a query error into zero, or report an invented observation-window count.

## Required local attribution check

Use current `Sources/SpeakCore/ProductAnalyticsDimensions.swift` and `ProductAnalytics.swift`, `Sources/SpeakApp/SwitchingLiveTranscriber.swift`, `SherpaOnnxRuntimeManager.swift` and `LocalPostProcessingModelManager.swift` to explain attribution boundaries. Current typed dimensions are model_family/engine_type/provider_type; family Parakeet includes FluidAudio and sherpa builds. Nemotron/Zipformer are annotated sherpa-backed. The broad local/streaming prefix is insufficient because native FluidAudio/WhisperKit routing precedes the generic sherpa branch. Record exact file/symbol references, not a blanket count of prefix selections.

The sherpa path still creates a venv and installs sherpa-onnx1.13.2, but independent local postprocessing also invokes Python/llama-cpp-python and shared process helpers. Any future acceptance must say no Python in the sherpa path, not no Python anywhere in SpeakApp. Preserve public model identifiers and helpers now.

## Bounded deliverable

Consolidate the existing evidence into a short reviewable decision brief containing verified project/time/schema, exact tools/queries actually executed (schema inspection only here), no count, attribution limits and the missing evidence needed. Reuse `posthog-1121-evidence.md`; do not invent a dashboard or duplicate connector calls solely for activity. Parent owns persistence/presentation and any authorised issue update.

If an existing additional source is offered later, qualify it first: production identity, event coverage across the exact requested 90-day UTC interval, event definition and historical schema, runtime-discriminating dimension and distinct-user/installation identity. Only then execute a read-only aggregate query. Report total qualified observations and distinct opted-in users separately; isolate ambiguous Parakeet rows and unknown fields rather than assigning them to sherpa. Do not export personal identifiers/transcripts when aggregates suffice. No new collection is within current permission.

Present choices without selecting one: retain current runtime pending evidence; pursue a separately scoped native-link implementation preserving models; or consider removal with precise affected-model migration only after Chris chooses. Explain that available evidence cannot estimate removal impact. Do not recommend removal based on an arbitrary 'near zero' threshold or the missing schema.

The qualification task is complete once the unavailable-data result and boundaries are presented. The runtime decision remains pending Chris and/or a qualified source; this is an explicit reviewed scope limit, not a request to stop already-authorised read-only work. A future native-link branch needs official release/API/checksum/platform research, fixture decode and real Mac tests; a future removal branch needs exact model ownership, selection migration and public API checks. Those tasks are not pre-authorised by this plan and should receive their own executable plan after the branch choice. No device build is necessary to validate this evidence-only deliverable, and none is claimed.
