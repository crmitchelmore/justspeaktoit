# #661 executable plan: correct keyboard verification and release documentation

Decision: NARROW to a small documentation-only PR plus retained physical handoff qualification. No keyboard/framework changes, build-flag changes, release dispatch or inferred rollout approval. Read reviews/661.json and current #991 source evidence. Old comments about #678/#719 blockers and manual TestFlight flags are historical; do not reopen those blockers. Documentation completion does not close the remaining physical-device obligation.

## Source contract to document

Current `.github/workflows/release-ios.yml` is a reusable workflow_call with one required `manifest` input. It checks out/loads the immutable release manifest and sets TUIST_IOS_KEYBOARD=1 and TUIST_IOS_KEYBOARD_DIRECT_CAPTURE=0. The extension ships in handoff mode; there are no current include_keyboard or enable_direct_capture dispatch inputs. Project.swift keeps independent development-generation flags, which is not release authorisation. `KeyboardCapturePlanner.path` returns blocked without required Full Access/container access, then handoff when direct policy is disabled before evaluating microphone/Speech permission. #991's retained flagged direct shape is a compatibility experiment; its CI compilation does not establish supported extension microphone access.

Use `Sources/SpeakCore/Resources/ReleaseTrains.json`, Project.swift, scripts/release-train.mjs and `Docs/alpha-stable-release-trains.md` as the actual identity/release source. Stable app/group are com.justspeaktoit.ios / group.com.justspeaktoit.ios; Alpha app/group add .alpha. Keyboard bundle is the selected app bundle plus .keyboard, so Alpha is com.justspeaktoit.ios.alpha.keyboard. Never tell an Alpha tester to validate the Stable App Group. TestFlight distribution is Alpha-only in the current train documentation; Stable candidates follow their separate existing release gate. Do not alter those policies here.

## Bounded edits

1. `Docs/ios-keyboard-v2-design.md`: make present shipping status authoritative at the top and in Decision/Privacy/Open risks. Relabel the previous “unverified candidate” microphone premise and direct-path design as historical/experimental, superseded by the recorded #991 platform finding. Keep useful rationale without presenting an impossible current permission walkthrough as a prerequisite for shipping handoff. Describe the app-owned microphone and nonce-scoped App Group result delivery. Direct code still exists behind a flag; do not claim it was deleted. Avoid independently asserting an open Apple bug or universal OS guarantee from repository prose; attribute historical findings to #991 unless independently verified.
2. `Docs/ios-keyboard-mvp-verification.md`: rename the main physical gate to shipping handoff qualification. Move direct permission/happy-flow/interruption/memory sections into one clearly conditional historical experiment appendix. State its prerequisite: new supported platform evidence, separately scoped implementation if needed, dedicated physical qualification and explicit rollout approval. It is not a routine “run matrix then flip flag” step. CI compiling that shape alone cannot satisfy these conditions.
3. Keep Full Access, profile/language, host-edit safety, system limits and accessibility tests in the SHIPPING matrix where applicable. Correct Profile/mode quick-switch text that currently promises Local runs inside the extension: the disabled policy selects handoff; verify the actual profile/capability UI and chosen app model rather than inventing availability. Preserve ready/not-ready guidance, first-use setup, exact nonce/stale-result rejection, selected/caret text, separator/second utterance, composed Unicode, target switching, cancellation and interruption coverage. Current KeyboardHandoff lifetimes are request180seconds, transcription90seconds, result60seconds; cross-check before retaining those numbers.
4. Replace “Run Release iOS with include_keyboard/enable_direct_capture” steps with verification of an existing authorised manifest/build and a link to the release-train runbook. Show shipping development generation explicitly as TUIST_IOS_KEYBOARD=1 TUIST_IOS_KEYBOARD_DIRECT_CAPTURE=0 tuist generate --no-open, retaining the relevant simulator commands. Development generation is distinct from uploading a build. Keep archive/appex and entitlement inspection, but parameterize train-specific identifiers and use release evidence's IOS_APP_GROUP/BUNDLE_ID rather than an unconditional Stable value.
5. `Docs/ios-testflight-release.md`: correct Normal release, identifiers/profile repair, Physical-device checks, Build-based rollback and completion evidence to the same manifest/train contract. Remove nonexistent manual version/include_keyboard/enable_direct_capture instructions. Link current Alpha/Stable controllers rather than duplicate their scheduling/allocation machinery. Rollback requires an authorised source/policy change and a new correctly signed build through existing release process; no runtime removal of an installed extension and no ad-hoc reusable-worker dispatch. Preserve separate archive, Apple processing, tester assignment, installation and physical verification evidence.

Limit edits to those three docs unless an adjacent link would otherwise contradict the corrected flow. No changes to workflow YAML, Project.swift, entitlements, provisioning secrets, CI enablement or runtime guards. #1126/#1127 next-Alpha validation remains its own gate; this documentation task does not dispatch that Alpha.

## Review and remaining device acceptance

Statically check every documented input against workflow schemas, both train identity mappings against ReleaseTrains.json, release policy against worker env and planner code, local command/scheme names, heading anchors and relative links. Search the three files for include_keyboard, enable_direct_capture, “candidate”, “fallback”, “flip” and Stable-only identifiers; historical mentions may remain only when explicitly labelled, not actionable current instructions. Run Markdown/link checks already available; no new tests or native build is needed for documentation-only changes. Report source verification separately from device verification.

Retain one physical iPhone at the supported iOS17 floor and one on the tested current OS, Notes/WhatsApp on both and Safari on the newer device; include an iPad for layout/VoiceOver. Record exact build/source/manifest, train, entitlement, device/OS, host/version, tester and pass/fail/pending evidence. No extension permission prompt in shipping mode; no host mutation when blocked/not ready; actual handoff inserts the intended text once and preserves cursor/surrounding content. Check interruptions, secure/system-restricted fields, globe switching, touch targets and accessibility. Retain a two-minute memory observation with measured peak/resident/no-jetsam evidence; label the existing below60MB threshold as a project qualification target, not an Apple-guaranteed extension limit. Do not use direct-transcription memory assumptions to claim handoff measurements.

The documentation PR may complete after source/link review; #661 hardware acceptance stays open without real results. Parent owns authorised publication and subsequent rollout decisions. No fabricated device rows or automatic issue closure.
