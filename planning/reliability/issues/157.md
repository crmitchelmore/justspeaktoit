# Issue #157 — Add live capture health details to the HUD

**Status**: Approved  
**Date**: 2026-04-08

## Plan summary
- `CaptureHealthSnapshot` plain struct in `SpeakApp` (not `SpeakCore`)
- Owned by `HUDManager` as `@Published var captureHealth: CaptureHealthSnapshot`
- Fed by `MainManager` bindings via `HUDManager.updateCaptureHealth(_:)`
- Refresh triggers: `PermissionsManager.statuses`, `AudioInputDeviceManager`, `AppSettings.liveTranscriptionModel` only
- Static `ModelCatalog.LatencyTier` for v1 (no measurement infrastructure)
- Categorical error labels only; type boundary enforces this

## Reliability assessment
- **Blast radius**: HUD display layer only; audio capture and transcription logic untouched
- **Failure mode**: Stale/incorrect display if subscription fails — graceful, non-crashing
- **Rollback**: Standard mac-v* re-tag via Sparkle
- **Monitoring**: No new surface needed; Sentry covers crashes; static tier needs no measurement infra
- **Deployment**: Low-risk additive change; no new infrastructure or network paths

## Cross-role notes
- Security (Priya): categorical label enforcement via type boundary — aligns with blast-radius containment
- Architecture (Morgan): `setupBindings()` Combine pattern is well-established; cancellables lifecycle is covered

## Blockers
None — all resolved by maintainer at 2026-03-25T02:13:52Z and confirmed by Architecture/Quality/Performance/Security
