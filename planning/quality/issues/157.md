# Issue #157 — Add live capture health details to the HUD

**Status:** quality-approved
**Date:** 2026-03-25

## Summary
Add CaptureHealthSnapshot to HUDManager: mic permission, input device, transcription provider, static LatencyTier.

## Resolutions
1. **Ownership**: `CaptureHealthSnapshot` as a distinct `@Published var captureHealth` on `HUDManager`, in `SpeakApp` only. Architecture confirmed plain struct, value semantics, no references to source managers.
2. **Latency signal**: static `ModelCatalog.LatencyTier` for v1 (Product, Performance, Architecture all confirmed). No measured session store.
3. **Update triggers**: only on `PermissionsManager.statuses`, `AudioInputDeviceManager`, `AppSettings.liveTranscriptionModel`. Dedicated `updateCaptureHealth(_:)` method only — never piggyback on `updateAudioLevel`.

## Guardrails (non-blocking, from other roles)
- Architecture: plain struct, no ObservableObject, no source manager references
- Performance: dedicated `updateCaptureHealth(_:)` method, never on high-frequency path
- Security: categorical error labels only, never raw provider payloads

## Test surface
- Unit tests for `CaptureHealthSnapshot` value assembly (no mocking needed — plain struct)
- `HUDManager` tests: verify `captureHealth` updates exactly on the three publisher events

## Approval
Approved — all three blockers resolved by maintainer proposal + Architecture/Performance/Product confirmations.
