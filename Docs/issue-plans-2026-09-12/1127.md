# #1127 implementation plan: complete, discoverable tooling tests

Decision: NARROW; SOL medium after coordinating #1125/#1126 paths. Consolidate Node test location, run both existing Ruby suites and preserve Python checks. No language-port campaign, ASC client rewrite or release dispatch. Read reviews/1127.json and current Makefile/ci.yml; this repository has no root package.json at inspected base, so do not invent an npm task dependency.

## Small executable patch

Move `Tests/ReleaseNotesTests/release-notes.test.mjs` and `release-notes-catalogue.test.mjs` into `scripts/tests`, and move `scripts/release-train.test.mjs` there. Preserve test names/assertions and all fixture contents. Update imports from ../../scripts to ../, release-train imports from ./ to ../, and each import.meta.url-based update-release-notes-catalogue path. Search spawned command paths, process.cwd assumptions and temp-repository fixtures rather than changing only top-level imports. Remove the empty old test directory only when no tracked files remain.

Add `.PHONY: test-tooling` and documented Makefile target executing from repository root:

1. `node --test scripts/tests/*.test.mjs`
2. `ruby scripts/tests/release_apple_test.rb`
3. `ruby scripts/tests/create_ios_app_store_profile_test.rb`
4. `python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v`
5. `python3 scripts/generate-release-train-config.py --check`

Use normal failing command semantics; no continue-on-error or shell chain that hides an earlier failure. The Ruby profile suite is self-contained with fake transport and needs no credentials. It was omitted from CI, so run it before assuming it passes: report/fix a concrete small test harness or implementation defect it reveals with targeted evidence, rather than skipping assertions to make a new entry point green. Any substantive provisioning behaviour change is a separate review.

Replace ci.yml's split Test CI and release gates/Test release-notes generator command blocks with `make test-tooling` at the current tools-test position. Preserve lint/pin drift checks and runtime environment. Keep brand-assets' additional Mac build job invocation if it protects that lane; 'one canonical discovery command' does not prohibit a deliberate targeted regression check. Update any other real old-path workflow/docs/test references. No test-only support source is moved into shipping targets. Do not install Node/Ruby/Python tools globally or change their versions merely for a file move.

## Configuration ownership: optional small follow-up

The ticket conflates two JSON files. `Config/ReleasePipeline.json` contains orchestration enablement, versions, published baselines and Apple app IDs. `Sources/SpeakCore/Resources/ReleaseTrains.json` is identity/storage catalogue read by Project.swift and the Python Swift-projection generator. Keep both authorities distinct; generator --check already exists and must survive #1123 path updates.

The minimal test-coverage PR does not need a production loader change. If repeated release-train.mjs parsing is included, add one narrowly exported `loadReleasePipeline(path = 'Config/ReleasePipeline.json')` in release-train-lib.mjs and route only its three equivalent call sites through it. Preserve current working-directory behaviour for temp-repository tests, return parsed values unchanged and throw clearly for missing/malformed input. Do not add strict validation that rejects existing minimal fixtures or alter allocation/hash/approval rules as a hidden cleanup. Tests cover defaults/custom path, missing/invalid JSON and actual config values.

Ruby release-apple.rb and alpha-release.yml's enablement preflight also read ReleasePipeline. They remain legitimate separate-language consumers. Do not rewrite those merely to satisfy exactly-two-readers, change Python stamping/archive/profile gates, or port with-test-keychain. If no meaningful repeated-load defect/value emerges, defer the loader entirely and document the existing distinction. All `validateManifest` immutable source/tag/train/dependency/notes checks remain unchanged.

## Verification and handoff

Inventory pre-move Node test names and result counts from all three locations, then compare the canonical command's discovered original names. Report new support tests separately, and distinguish subtests from top-level tests so a numeric total cannot hide lost cases. Run both Ruby suites, all Python unittests and projection freshness locally where supported; these are offline and should not call Apple/GitHub/provider production. Run actionlint for ci.yml and normal required PR CI. Update existing workflow-source assertions rather than deleting them after command relocation; they should assert complete tooling coverage and preserved gates.

The code move/test-discovery change requires no native microphone/device run. CI execution must confirm the same runner's Node/Ruby/Python availability and all commands pass. If optional production config loading changes, explicitly mark release-path acceptance pending the next real Alpha and record its immutable tag/source/surface evidence when it occurs; the owner has prohibited verification dispatches. Existing Stable manifest approval and release-worker order stay unchanged. No claim that zero Python/Ruby or a green mocked suite proves Apple signing/upload parity. Submit a scoped conventional PR with exact moved suites, newly enabled profile coverage and any concrete dormant failure found; Beads availability limits remain honest.
