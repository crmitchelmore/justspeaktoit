# Issue #247 — Stance

**Title:** [Perf Improver] perf: cache NSRegularExpression in PronunciationManager TTS pipeline
**Status:** No action — not a planning issue

Issue #247 is bot-generated from the Daily Perf Improver workflow. Labels: `automation`, `performance`. No `planning:` labels. No Planning Kickoff comment. Issue is closed as `not_planned`.

Per operating constraints: do nothing when no `planning:` labels and no kickoff comment exist.

**Note for context:** The issue itself contains a technically sound proposal — caching `NSRegularExpression` instances in `PronunciationManager` to avoid recompiling patterns on each TTS call. Estimated waste: 100–300µs per compile × ~10 rules × multiple calls. The measurement strategy (XCTest `measure{}` block) is appropriate. If this is ever reopened as a proper planning issue, the performance case is already well-specified.
