# Issue #283 — fix(ios): add missing SpeakCore import — unblocks TestFlight releases

**Status**: Product-approved.
**Date**: 2026-04-08
**Author**: bravostation (COLLABORATOR)

## Summary
iOS TestFlight releases broken since March 26 due to missing `import SpeakCore` in `SpeakiOSApp/SpeakiOSApp.swift`. `OpenClawClient.Conversation` is used at line 93 but `SpeakCore` is not imported — only `SpeakiOSLib` (which does not re-export `SpeakCore`).

## Decision
Approved immediately. Problem evidenced, scope is one line, release pipeline unblocked. Other roles still pending (`needs-security`, `needs-performance`, etc.).

## Labels applied
- `triage:product-fit` added (prior run)
- `planning:product-approved` added; `planning:needs-product` removed (this run)
