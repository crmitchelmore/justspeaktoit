# Issue 283 — fix(ios): add missing SpeakCore import

## Stance
Approved immediately.

## Rationale
One-line compile-time fix: adds `import SpeakCore` to `SpeakiOSApp.swift`. Zero runtime cost. No hot-path involvement. Import statements carry no execution overhead.

## Resolved
No performance blockers existed. Confirmed file has no `import SpeakCore` at lines 1-3; fix is exactly as described.

## Label actions
- Added: planning:performance-approved
- Removed: planning:needs-performance
