# Issue #149 — Portable planning-memory pattern after signed-commit correction

**Status:** needs re-review (post-correction round 2)
**Last updated:** 2026-03-25

## Summary
Issue exists to define the reusable rollout contract for planning memory on repos with signed-commit enforcement.

## Verified facts
- Contents API (`PUT /repos/{owner}/{repo}/contents/{path}`) produces `verified: false, reason: unsigned` in this repo. NOT a signed-commit-safe write path.
- Architecture's comment 4122824374 still incorrectly references Contents API as GitHub-verified. That comment is stale and must not resolve the portability question.
- Maintainer explicitly cleared all prior approvals after the Contents API correction.
- Three factually valid options remain: (1) planning/* branch exemption, (2) workflow commit signing, (3) dedicated planning-memory repo.

## Quality stance
- Rollout contract has five checkpoints: pre-flight check, choose write path, verify end-to-end, document chosen path, correction protocol.
- Contents API is explicitly excluded from the contract.
- Cannot finalize the contract until Architecture and Security agree on which option is the portable first recommendation (exemption vs signing).

## Open dependency
- Architecture must correct its stance (Contents API is out) and settle the portable default.
- Security must confirm whether workflow commit signing is preferred over branch exemption for strict orgs.

## Approval status
Not yet. Approve once Architecture gives a corrected recommendation and Security confirms preferred path.
