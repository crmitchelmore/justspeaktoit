# PR #181 — fix(agentic-workflows): ignore closed PR comments

## Status
Architecture-approved (first pass, 2026-03-25)

## Linked plan issue
Issue #180 — `planning:ready-for-dev`, all five roles approved.

## Summary
Adds `github.event.issue.state == 'open'` guard to the `issue_comment` condition in all five role `.lock.yml` files, the bot-follow-up dispatcher, and the corresponding `.md` sources. No app code touched.

## Architecture assessment
- Entirely `.github/`-scoped — no module boundaries or app layers affected.
- Symmetric guard applied across all 5 roles + dispatcher; no role can still fire on closed PRs.
- `.md` sources updated alongside `.lock.yml` files — code-as-documentation pattern maintained.
- Minimum change to fix the confirmed failure mode (closed PR #161 firing on `issue_comment`).
- Acceptance criteria met exactly as specified in issue #180.

## Open concerns
None.
