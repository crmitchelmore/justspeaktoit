# PR #246 — Security Review

## Status
**No action: MCP auth gap — all reads return empty arrays**

## History
- 2026-04-07: GitHub context shows `issue-number: #246`, `pull-request-number: (empty)`. PR #246 does not appear in the open PR list. Direct `github-pull_request_read` and `github-issue_read` both returned empty arrays. Same private repo / MCP auth gap pattern. Per protocol, `issue_comment` triggers only warrant action when the comment belongs to a pull request. Since #246 is not visible as an open PR, took no action.
- 2026-04-09: Re-triggered via issue_comment (comment-id: 4212471016). All MCP reads (get, get_comments, get_reviews) still return empty arrays. No action per documented pattern.
