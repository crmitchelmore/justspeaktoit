{
  "name": "justspeaktoit-landing",
  "version": "1.0.0",
  "description": "Landing page for JustSpeakToIt - Voice-first transcription for Mac & iOS",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run serve.ts",
    "build": "mkdir -p dist && cp index.html dist/",
    "preview": "bun run serve.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}