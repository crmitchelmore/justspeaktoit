# Issue #283 — fix(ios): add missing SpeakCore import — unblocks TestFlight releases

**Status**: Product-validated. Ready for /doit.
**Date**: 2026-04-08
**Author**: bravostation (COLLABORATOR)

## Summary
iOS TestFlight releases broken since March 26 due to missing `import SpeakCore` in `SpeakiOSApp/SpeakiOSApp.swift`. `OpenClawClient.Conversation` is used at line 93 but `SpeakCore` is not imported — only `SpeakiOSLib` (which does not re-export `SpeakCore`). Confirmed by inspecting the file.

## Decision
Approved immediately. Problem evidenced, scope is one line, release pipeline unblocked.
No scope guardrails needed.

## Labels applied
- `triage:product-fit` added
