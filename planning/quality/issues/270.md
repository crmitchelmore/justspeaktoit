# Issue #270 — fix(ios): Apple live transcription clears text after speech pause

**Status:** quality-approved
**Labels at approval:** planning:in-discussion, needs-security, needs-performance, product-approved, architecture-approved, design-approved, reliability-approved, quality-approved

## Summary
iOS SFSpeechRecognizer fix. Accumulated text lost after speech pause. Fix surface: `iOSLiveTranscriber.swift`.

## Failure modes addressed
1. **Threshold miss** — `commitIfImplicitReset` only fires when `count >= 10` AND drops > 50%. Short and gradual resets bypass it. Fix: loosen threshold (>= 1) and review the ratio guard.
2. **Error path data loss** — `handleRecognitionResult` error branch (lines 276-286) returns without committing `lastFormattedString`/`latestResult`. Fix: commit `lastFormattedString` to `committedText` before returning, guarded by `!isShuttingDownRecognitionTask`.

## Quality blockers (resolved)
1. **Which failure modes?** — Resolved: Architecture (Morgan) and Reliability both confirmed both failure modes must be addressed.
2. **Error path commitment** — Resolved: Morgan confirmed commit-before-return in error path, guarded by `!isShuttingDownRecognitionTask`.
3. **Verification story** — Structural limitation: no SpeakiOSTests SwiftPM target. Resolved with guardrail: PR must include device verification steps/log evidence.

## Guardrails for PR
- `isShuttingDownRecognitionTask` must gate the error-path commit to protect intentional cancel
- Text segment join must not produce double-space at boundaries (Riley's design flag)
- PR must show device verification evidence (log output or manual test checklist)

## Cross-role patterns
- Architecture, Reliability, Quality all independently identified same two failure modes — strong convergence
- No iOS unit test infrastructure (SpeakiOSTests) — device testing is the verification story for iOS-only code
