# PR #181 — fix(agentic-workflows): ignore closed PR comments

**Linked issue:** #180 (approved)
**Status:** Performance-approved

## Summary
Pure CI guard change. Adds `github.event.issue.state == 'open'` check to all PR role-review workflow triggers and the PR bot follow-up dispatcher. Prevents spurious workflow runs on closed/merged PRs.

## Performance assessment
No runtime code changes. No app hot path affected. Reduces unnecessary workflow fan-out (consistent with prior agentic-workflow approvals #174/#175/#176/#177/#180). Pattern is identical to previously approved PRs.

## Outcome
Approved immediately. No guardrails needed.
