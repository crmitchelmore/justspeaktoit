# Issue #174 — Port final career-framework agentic workflow pattern

## Summary
Pure CI/workflow change: update GitHub Actions workflows, add persona.md files, remove redundant triage layer, fix PR review routing, update docs.

## Performance stance
**Approved immediately.** No runtime app code touched. Zero impact on the hot path (audio capture, transcription, HUD).

## Resolved blockers
None needed — entirely outside app performance scope.

## Outcome
`planning:performance-approved` added on first review.
