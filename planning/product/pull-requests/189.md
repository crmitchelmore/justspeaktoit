# PR #189 — fix(mac): prevent Data.subdata crash from non-zero startIndex

## Summary
Crash fix: `Data.removeFirst(_:)` shifts `startIndex` instead of reindexing to zero. `subdata(in: 0..<3200)` then crashes with `EXC_BREAKPOINT` when startIndex != 0. Fix replaces all four `pendingPCMData.removeFirst(offset)` calls with `Data(pendingPCMData.dropFirst(offset))`.

Affects both Modulate and AssemblyAI live streaming audio processors.

## Labels on intake
plan-review:in-discussion, needs-product, needs-security, needs-performance, needs-quality, needs-architecture, needs-design

## Product stance
**BLOCKED — missing linked plan issue.**

No `Plan issue: #<n>`, `Closes #`, `Fixes #`, `Resolves #`, or `Refs #` in PR body. Cannot approve without a verified planning issue. However, this is a surgical crash fix — the path to yes is lightweight: open a bug issue (or reference an existing one) and link it in the PR body.

## User value
Clear: prevents crash during live transcription for users of Modulate or AssemblyAI providers. Core capture moment broken without this fix.

## Open questions
1. Does a GitHub issue already exist for this crash? If so, link it.
2. If not, the maintainer should open one and add `Refs #<n>` to the PR body.

## Last updated
2026-03-26
