# Issue #174 — Port final career-framework agentic workflow pattern

## Summary
Port the final career-framework agentic workflow pattern to justspeaktoit. Entirely `.github/` changes; no app code touch.

## Stance
**Approved.**

## Key structural facts verified
- All 5 planning role workflows already fire on `issues: [opened]` — the kickoff dispatch chain in `bot-follow-up` is redundant for initial round.
- `bot-follow-up` hardcodes `### 🗂️ Planning Kickoff` as the trigger to dispatch all 5 roles (line 51/69). When kickoff is removed, this explicit dispatch block should also be removed or its heading updated.
- `bot-follow-up` line 65: skips dispatch if no planning labels AND no kickoff comment — once Product sets labels on open, this check works correctly for follow-up rounds.
- `issue-planning-reconcile.yml` handles `workflow_run` completions only; PR plan-review reconcile needs `pull_request` + `issue_comment` triggers added.
- No `pr-review-*` workflows exist yet — they must be created from career-framework.

## Guardrails
- Remove or update `bot-follow-up` kickoff dispatch block when `issue-planning-kickoff` is retired.
- Product role must set `planning:in-discussion` and all `planning:needs-*` labels on first open pass to preserve the follow-up chain.
