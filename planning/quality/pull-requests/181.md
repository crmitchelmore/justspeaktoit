# PR #181 — fix(agentic-workflows): ignore closed PR comments

**Plan issue:** #180 (planning:ready-for-dev, all roles approved)
**Status:** approved
**Date:** 2026-03-25

## Scope
Workflow-only: 5 role lock files + bot-follow-up dispatcher + 5 source .md files.

## What changed
Added `github.event.issue.state == 'open'` to `issue_comment` guard in both `pre_activation` and agent jobs across all role lock files; mirrored same guard in bot follow-up dispatcher.

## Plan alignment
Exactly matched all 3 acceptance criteria from #180: closed PR comments blocked, open PR comments still trigger, pull_request/workflow_dispatch untouched.

## Quality assessment
- Guard pattern is explicit, API-backed, and consistent (8 additions across 4 other lock files, 2 in quality lock, 1 in bot follow-up)
- Both compiled lock files and source .md files updated — no recompile drift
- Failure mode confirmed: traced to closed PR #161
