# Issue #246 — Incremental transcript append in DeepgramLiveController.handleTranscript

**Status:** Product approved (2026-04-09)
**Type:** Automated perf improvement (daily-perf-improver bot)

## Stance
Approved. Well-scoped, evidenced, no user-facing risk.

## Evidence
- Line 653 in TranscriptionManager.swift: `fullTranscript = finalSegments.map(\.text).joined(separator: " ")` — confirmed O(N) on every final segment commit.
- Fix is incremental append; identical output, lower allocation per call.
- AssemblyAI path (line 1195) noted as separate concern; replace-semantics handled separately.

## Other role approvals at time of product approval
- architecture-approved ✓, design-approved ✓, reliability-approved ✓
- Still pending: security, performance, quality

## Qualifies for fast-track per principles
- Problem evidenced in code ✓
- Scope bounded to single function ✓
- Acceptance criteria testable (XCTest measure block) ✓
- No user-facing product risk ✓
