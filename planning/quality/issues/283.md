# Issue #283 — fix(ios): add missing SpeakCore import

**Status:** Approved (first pass)

## Summary
`SpeakiOSApp.swift` line 93 references `OpenClawClient.Conversation` (type lives in `SpeakCore`) but only imports `SpeakiOSLib`. Fix: add `import SpeakCore`.

## Verification story
Build failure is the test. TestFlight CI will pass once import is present. No new tests needed.

## Quality assessment
- One-line fix, zero scope creep
- Correct root cause: SpeakiOSLib does not re-export SpeakCore
- No doc impact
- Confirmed OpenClawClient in Sources/SpeakCore/OpenClawClient.swift

## Approval rationale
Trivially verifiable by build. No quality concerns.
