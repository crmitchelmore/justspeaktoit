# #1110: parity-gated Modulate fold

NARROW/IMPLEMENT with SOL medium after #1104 options and #1105 shared contracts land. Read repository guidance, review 1110 and prerequisite diffs; reuse #1106–1109 patterns, not their provider control frames. Use an isolated branch, parent integration and normal CI/review. Beads is absent. Preserve actual dictation, features and transparent estimated cost before deleting code.

## Behaviour contract

| Dedicated Mac behaviour | Required shared outcome |
|---|---|
| Four snapshotted settings; diarization defaults true on Mac | Existing LiveClientOptions.modulate and Mac settings projection; legacy core defaults remain all false |
| First PCM frame has streaming WAV header; 100 ms frames/50 ms final residual | Client-side ordered framing/header exactly once, including converter tail |
| Drain sends, user grace, empty text EOS, wait done/two seconds | Graceful async finish, no immediate socket cancellation |
| Utterance callback updates cumulative formatted display then emits raw utterance boundary | Same order and one boundary per utterance; done/finish creates no extra boundary |
| Speaker labels only with multiple distinct speakers and diarization enabled | Preserve exact current formatting, including earlier text relabelling when second speaker appears |
| Timed segments, provider duration, raw utterance JSON and estimated cost | Single #1105 result snapshot; no separate metadata protocols |
| Terminal streaming error immediately stops capture/releases mic | Generic cleanup only for snapshot-confirmed terminal error, preserving failed-run data |

Important issue correction: `ModulateFeatureConfiguration` and `ModulateUtterance` are used by the retained **batch** provider. They cannot simply be deleted. Actual formatting only adds speaker labels; emotion/accent are stored metadata, not inline labels. Do not invent formatting features because the review says “rich metadata.”

## 1. Share existing pure representation and reuse options

Files: `Sources/SpeakCore/ModulateLiveClient.swift`, existing `LiveClientOptions.swift`, new focused `ModulateTranscription.swift` if needed, `LiveTranscriptionClientFactory.swift`, and `Sources/SpeakApp/ModulateTranscriptionProvider.swift`.

Keep #1104's exact old and new public client initializers/factory signatures. Pass existing `LiveClientOptions.modulate` and stop timing through a new required-options overload if necessary, retaining forwarding symbols. SpeakCore must not import AppSettings or read defaults. The Mac settings adapter already maps all four flags; verify saved values and defaults. Do not use ModulateLiveOptions.none to overwrite Mac diarization's default true.

Move the existing Codable/Equatable/Sendable `ModulateUtterance` into SpeakCore with fields/CodingKeys unchanged and the public initializer/properties required by the Mac batch adapter: utteranceUUID, text, startMs, durationMs, speaker, language, emotion, accent. Do not add invented PII fields; server text/raw known fields stay as currently decoded.

Share pure formatting on existing `ModulateLiveOptions` or one small internal/shared formatter; preserve `formattedTranscript` and `segmentText` semantics exactly. Keep Mac `ModulateFeatureConfiguration` as a thin defaults/batch adapter with its existing API (including multipartFields), forwarding pure formatting/options conversion to the shared implementation. This preserves batch tests and eliminates competing live flag ownership without a broad batch rewrite. Shared legacy transcript callbacks remain plain cumulative text for iOS; snapshot text supplies the formatted Mac projection. No platform check in transport is required.

## 2. PCM and bounded finalisation

Add `FinalizingStreamingTranscriptionClient` conformance with true flush capability. Use upstream fake socket/deadline seam, one session identity and one ordered outbound path. Preserve current endpoint/query ordering, api_key authentication and 30-second handshake request timeout; never log the credential-bearing URL.

Both current clients drop audio when no usable task exists. Reuse `StreamingAudioPreroll` with its existing default cap/eviction semantics to retain first words; drain automatically on usable transport before later chunks, and cap send admission on a stalled connection. No speculative server-ready event or fixed startup sleep. Stop-before-connect lets queued work progress within the send/startup deadline; abort cancels it.

Frame PCM16 mono at configured rate into 100 ms chunks (3200 bytes at 16 kHz), retaining a residual. On finish pad nonempty residual to 50 ms; empty audio emits no padded silence. Prefix the streaming WAV header to the first actual PCM frame only, preserving RIFF/WAVE/fmt/data fields, little-endian sizes/sample rate/byte rate and UInt32.max streaming sizes. Do not add a WAV header to EOS. Header ownership is session-local and cannot race concurrent audio sends.

Finish sequence: arm shared waiters and reject new audio; flush residual after converter drain; await admitted sends up to 1.5 seconds; receive through configured user grace; enqueue exactly one **empty text frame** EOS; keep reading delayed utterances and `done.duration_ms` for up to the dedicated two-second budget; snapshot then cancel. Bound EOS completion as part of send work, total below manager's ten-second safety timeout at maximum valid grace. A done/error arriving synchronously on EOS must resolve already-armed waiters. Return full plain cumulative text (or nil for no utterances); Mac snapshot separately provides formatted text. Do not emit the same final both via callback and finish return. Repeated finish callers join; immediate `stop()` aborts and resolves each once.

Guard every receive/send/deadline/restart mutation by captured run. done outside finish may deliver its normal cumulative final once, then cleanup without sending EOS recursively. During finish, done stores duration and resolves only after all fields are parsed. Unknown events remain harmless; malformed known payload/provider errors surface usefully and resolve finish. Keep accepted utterances on timeout/error.

Primary docs retrieval on 12 September 2026 was blocked: the repository's [official documentation URL](https://www.modulate-developer-apis.com/web/docs.html) redirects to platform.modulate.ai and could not be fetched. Preserve wire behaviour evidenced by both existing clients (WAV prefix, empty text EOS, utterance/done/error) and record provider validation outstanding. Do not infer a production endpoint migration from a documentation redirect. Current price verification is likewise not established; the existing estimate below is retained, not advertised as newly verified pricing.

## 3. Snapshot, boundaries and error cleanup

Conform to #1105's ONE `StreamingTranscriptSnapshotProviding`; use its confirmedText, pendingInterim, segments, confidence, duration, cost, rawPayload and isTerminal fields. No pending interim exists for discrete complete utterances. Snapshot confirmedText is the current feature-formatted cumulative transcript; legacy callback text remains plain. Timed segments use startMs/1000 and (startMs+durationMs)/1000 and current shared `segmentText`, so earlier segments gain labels when a second speaker is detected. Preserve genuine identical utterances; never text-dedupe them.

Use provider done duration when available, otherwise the supplied captureDuration. Encode the existing `ModulateLiveCapture` shape (`duration_ms`, utterances) with the same known fields; snapshots must not log that private JSON. Estimate positive-duration streaming cost exactly as today: Decimal(durationSeconds/3600) × Decimal("0.06"), currency USD, inputTokens Int(durationSeconds), outputTokens zero; nil for no positive duration. Move this single streaming estimate into a shared helper and point any retained batch-provider streaming-rate branch at it without changing batch/English-fast rates. No price update or provider feature surcharge assumptions.

The #1105 callback wrapper captures immutable snapshot text before MainActor dispatch. Emit the existing explicit boundary callback only after each utterance's normal transcript callback, passing utterance.text, not formatted cumulative history. During finish accept boundaries for newly arrived utterances once; generic final adoption/done is suppressed as a boundary because the capability owns them.

Set snapshot isTerminal true before delivering a terminal error. In the generic shared controller error callback capture that state/snapshot before the actor hop; for true only, stop engine/remove tap/reset processor, release preferred input and retain the failed-run snapshot for any in-progress finish/result. Do not call full failed-start cleanup that erases transcript state. Deliver didFail once; an in-progress stop joins/resolves existing finish and must not produce duplicate completion. Existing providers default false, so recoverable errors are not reclassified. Test this narrow cleanup through the upstream controller seam; no Modulate type checks.

## 4. Route/removal and tests

After native focused tests pass, route `modulate/` to sharedClient in `SwitchingLiveTranscriber`, remove ControllerSet.modulate/construction/all entry, and delete `ModulateLiveController.swift`. Delete `ModulateLiveTranscriber`, its factory and streaming-only envelope/done/error helpers from `ModulateTranscriptionProvider.swift` after reference search. Keep the provider, batch response/validation/multipart/error helpers and thin feature adapter. Remove local utterance type only after shared type consumers compile.

Keep **all seven** existing `ModulateIntegrationTests` and MockURLProtocol: prefix selection, query flags, persisted defaults, two formatting cases, validation/redaction and registry. None currently directly tests ModulateLiveTranscriber. Move/duplicate pure formatting assertions into new `Tests/SpeakCoreTests/ModulateLiveClientTests.swift` only when they exercise the newly shared production formatter; retain adapter assertions for batch compatibility.

Required additional fixtures:

- Legacy/all-false and configured mixed/all-true query parity; Mac default diarization true; options frozen during run.
- Tiny/large PCM, residual/tail, single exact WAV header, no header/EOS mixup, bounded preroll, held PCM completion before EOS, custom grace and stop during startup.
- Two utterances/two speakers and repeated identical utterance: correct cumulative display, retroactive labels, timed segments, raw field JSON, standalone boundary order once; done adds none.
- Delayed utterance then done after EOS; synchronous done, silence, timeout, malformed known event, server error, failed send, concurrent finish/abort and restart with stale callbacks. Final result delivered once; terminal errors release capture promptly.
- Provider duration versus capture fallback, zero duration, exact known estimate and raw duration consistency; plain legacy callback versus formatted Mac snapshot.

Extend contract/snapshot/controller/routing suites and preserve other provider defaults. Linux diff/reference checks are static only. Apple Swift gates: focused suites, make lint/build/test, exported API check against PR base and existing ci.yml iOS tests/build. Real Mac with authorised existing credentials: immediate/last words, short/silent recording, multiple speakers, all configured flags, grace, cancel/restart/error and mic release; verify History text/segments/cost/raw metadata and Live Polish cadence. Real iOS smoke ensures plain display and graceful finish remain correct. No credit activation, unverified device claims, direct main or release publication; optional new formatting/features remain out of scope.
