# Issue #256 — Test Improver: error description, header redactor, API validation tests

## Status: APPROVED (first pass)

## Summary
Adds three new test files to `Tests/SpeakCoreTests/`:
- `ErrorDescriptionTests.swift` — `TranscriptionProviderError` (3 cases) + `OpenClawError` (4 cases) errorDescription strings
- `SensitiveHeaderRedactorTests.swift` — isSensitiveKey, redactValue, redactSensitiveHeaders
- `APIKeyValidationResultTests.swift` — factory methods, Equatable, updatingOutcome, header redaction via debug snapshots

## Verified against source
- `TranscriptionProviderError`: 3 cases (apiKeyMissing, invalidResponse, httpError) in TranscriptionProviderRegistry.swift
- `OpenClawError`: 4 cases (encodingFailed, serverError, notConnected, invalidResponse) in OpenClawError.swift
- `SensitiveHeaderRedactor`: public enum with 3 public methods in SensitiveHeaderRedactor.swift
- `APIKeyValidationResult`: outcome (success/failure), factory methods, updatingOutcome, debug snapshot in APIKeyValidationResult.swift

## Issue description imprecision
Issue table mentions "isValid flag, displayMessage, empty/whitespace/prefix/length validation" — these don't exist in source. The commit message is correct: "factory methods, Equatable outcome, updatingOutcome, and automatic header redaction in debug snapshots". Not a blocker — tests match real source API.

## Quality notes
- Pure value-type assertions, no mocks, no async — reliable and fast ✅
- Naming convention matches repo pattern (test<Behaviour>_<component>_<expectation>) ✅
- Run commands provided ✅
- No docs impact — test-only addition ✅

## Resolved by
First-pass inspection of source files + existing test naming conventions.
