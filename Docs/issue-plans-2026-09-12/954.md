# #954 remaining acceptance plan: native Control setup and action hints

Decision: NARROW to recorded physical-device and accessibility qualification. No new implementation PR. The requested setup/hints already exist through #1050/#1035 integrated in main f8af0b11. Read reviews/954.json and current source, not the stale comment describing unmerged #1025. Record results in the existing issue/owner acceptance record; parent handles any authorised publication. Do not trigger a release to run this plan.

## Existing code to qualify

After #1115 extraction, setup lives in `Sources/SpeakiOS/Views/Settings/HardwareTriggerSettingsView.swift`, especially `nativeControlSetupSection`; older source has it inside SettingsView.swift. It gates native Controls to iOS18+, gives Action Button Controls-picker and press-and-hold instructions, Control Center placement, and distinct bottom Lock Screen placement instructions. Shortcut/Back Tap remain secondary. Destination text describes the selected result route and does not add a redundant Shortcuts Copy step. First-use permissions/unlock caveats stay truthful.

`JustSpeakToItWidgetExtension/JustSpeakToItWidgetExtensionControl.swift` uses `CaptureSurfaceKind.transcriptionControl`, `ToggleTranscriptionControlIntent`, destination-neutral description, and `controlWidgetActionHint`. Source intentionally uses Start for represented on state and Stop for represented off state: assess the actual system UI, not a naive inversion of the current recording Boolean. Preserve the kind identifier so existing placements remain valid. `CaptureSurfaceRefresher.recordingStateChanged` already targets reloads; #941/#1035 own state behavior and #943 owns capture ownership. Do not add a second state-publication path or make SetValueIntent ignore its requested value.

## Device walkthrough

Use existing authorised Alpha/TestFlight builds containing the integration. Cover a supported physical iOS18.x device and iOS26.x device; Action Button rows need capable hardware. Record model, exact OS, installed build/source, initial permission state and existing/new placement for every result. Unavailable hardware rows remain pending.

| Journey | Observable acceptance |
|---|---|
| Follow Action Button instructions from Settings | Actual Controls picker can locate Transcribe Voice; press-and-hold guidance matches the device; both Start/Stop action hints describe the resulting action |
| Follow Control Center instructions | Add, use and retain the Control; start then stop produces one session and current visible state |
| Follow bottom Lock Screen instructions | Correct bottom controls editor, distinct from accessory widgets; any unlock or foreground transition is recorded accurately |
| Existing placement after upgrade | Control identity remains connected and responds without removal/re-addition |
| Shortcut and Back Tap guidance | Existing shortcut executes with selected destination; no extra Copy action is required and no status string replaces transcript |
| Change state through app or Live Activity | Control's next presentation/action agrees with actual capture state after integrated state updates; record timing and stale displays |
| First use / denied permission | Guidance honestly covers permission or unlock prompts; do not promise locked background microphone access from source inspection |
| Destination choices | Clipboard, Copy & Polish, History Only and applicable Auto behavior match current guidance and #934 raw-once contract |

If testing a build containing #994, also verify hardware guidance exposes effective local-only/expiry settings and the new Universal Clipboard opt-in location. This conditional check does not make #954 wait for #994 or retrofit old builds' text. #953 background-start qualification and #955 widget work remain separate owners.

Inspect setup sections with largest accessibility Dynamic Type and VoiceOver: readable step order, meaningful headings/link/button labels, nontruncated choice text and separate Lock Screen placement explanations. Record screenshots only with synthetic, nonprivate content.

No code testing is necessary merely to repeat source inspection. Reference existing exact-build Apple CI as supporting evidence; if the walkthrough identifies an actual text/availability/hint mismatch, propose the smallest correction and require native app/extension build plus focused UI validation. Do not turn a Control state failure into a parallel recorder rewrite. Completion requires real device/accessibility observations. With no devices, report implementation covered, device gates pending; never close acceptance from Linux/static checks.
