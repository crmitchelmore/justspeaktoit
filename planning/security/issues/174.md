# Issue #174 — Port final career-framework agentic workflow pattern

**Status**: needs-security (pending one answer on PR reconcile trigger scope)

## Summary
Workflow/automation cleanup on a private repo. Ports the career-framework planning + PR-review pattern. Removes the general-purpose triage bot in favour of Product-on-open validation. Adds named personas to role memory.

## Security landscape
- Private repo: external spam/abuse threat is minimal; spam-detection loss from removing `issue-triage.lock.yml` is not a material concern.
- Named `persona.md` memory files: same isolation pattern as existing memory — stored in `planning/*` branch, written only by agent, not derived directly from issue content. Low risk.
- PR plan-review restricted to PR threads only: scope-narrowing improvement.
- Issue planning reconcile currently operates on label state only (not comment content) — safe even if `issue_comment` added as trigger.
- **Open question**: The new PR plan-review reconcile will handle `issue_comment` completions. Need confirmation it operates on label state (not comment content) and what its write permissions are. If it can apply `plan-review:ready-to-merge` or similar based on comment content parsing, that's a trust-boundary question.

## Open blockers
1. Confirm PR plan-review reconcile reads label state (not comment text) to decide merge-readiness, and that it runs with least-privilege write scope.

## Cross-role notes
None yet. No other planning comments on this issue.
