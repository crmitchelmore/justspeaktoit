# PR #191 — fix(agentic-workflows): disable failure issue creation on all workflows

## Summary
Workflow-only change. Sets `GH_AW_FAILURE_REPORT_AS_ISSUE: "false"` in all 31 `.lock.yml` files and adds `report-failure-as-issue: false` to corresponding `.md` source files. Frontmatter hashes updated. 62 files total, no Swift changes.

## Quality lens
- Change is mechanical and consistent across all 31 workflows
- Lock files are compiler output — hash updates expected
- No test coverage needed (no implementation surface)
- Verification: grep for `GH_AW_FAILURE_REPORT_AS_ISSUE: "true"` should return 0 results
- **Blocker: No plan issue linked** (no `Plan issue:`, `Closes #`, `Fixes #`, `Resolves #`)

## Status
**Not approved** — missing plan link. Consistent with prior pattern on PRs #186, #189, #161.

## Cross-role
No cross-role dependencies noted. All `needs-*` labels present. No prior comments.
