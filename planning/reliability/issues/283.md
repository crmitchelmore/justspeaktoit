# Issue #283 — fix(ios): add missing SpeakCore import

**Status**: approved  
**Date**: 2026-04-08

## Summary
One-line compile fix: add `import SpeakCore` to `SpeakiOSApp/SpeakiOSApp.swift`. All iOS TestFlight releases have been broken since March 26.

## Verification
- `OpenClawClient` is `public final class` in `Sources/SpeakCore/OpenClawClient.swift` ✓
- `SpeakiOSApp.swift` line 93 uses `OpenClawClient.Conversation?` without importing `SpeakCore` ✓
- Fix is purely compile-time correctness; no behavioral change

## Reliability assessment
- Blast radius: iOS only; macOS unaffected
- Deployment gate: TestFlight (manual workflow dispatch + App Store review chain)
- Rollback: simply do not promote from TestFlight
- No new failure modes introduced
- No monitoring changes needed

## Side note
`fix(ios):` commit type will trigger macOS auto-release (known pipeline behavior per principles). Version bump but no macOS behavior change. Acceptable.
