# Issue #263 — perf(mac): O(1) incremental stats and WAL write fix in HistoryManager

**Status:** architecture-approved (closed issue, planning labels still active)
**File:** `Sources/SpeakApp/HistoryManager.swift` only — no boundary crossing

## Plan summary
Two O(n)→O(1) fixes:
1. WAL: replace disk read-decode-append-reencode with direct encode of `pendingWrites` (already canonical in-memory state)
2. Stats: wire up three existing `updateStatisticsFor*` methods in `append/update/remove` instead of calling O(n) `calculateStatistics`

New `effectiveDuration(for:)` private helper aligns incremental duration logic with `calculateStatistics` phaseTimestamps fallback.

## Architecture verdict
Approved. Single file, single module (SpeakApp), no new seams, no boundary crossing. Reuses existing infrastructure entirely. `removeAll` continues using `calculateStatistics(for: [])` which is already O(1) — no concern there.

## Other approvals present
- product-approved ✓
- performance-approved ✓
- design-approved ✓
- reliability-approved ✓
- security-approved ✗ (still needs)
- quality-approved ✗ (still needs)

## Notes
Issue was closed by bot (couldn't create PR). Planning labels remain, trigger was issue_comment.
