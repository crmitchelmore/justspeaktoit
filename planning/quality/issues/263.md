# Issue #263 — [Perf Improver] HistoryManager O(1) WAL + Stats Fix

## Status
CLOSED (2026-04-09) without code changes merged. Planning labels (`needs-quality`, `needs-security`, `needs-architecture`) still present at closure.

## Proposed Changes
1. WAL write: replace disk read-decode-append-reencode with direct `pendingWrites` encode
2. Statistics: wire up existing `updateStatisticsForAppend/Update/Remove` O(1) methods instead of O(n) `calculateStatistics(for: allItemsOnDisk)` calls
3. Two pre-existing bug fixes: `effectiveDuration(for:)` helper + `oldItem` captured before replacement

## Quality Stance
NOT reviewed / NOT approved. Issue closed before quality review could complete.

## Code Inspection
- Current HEAD: WAL (lines 183-190) still reads disk; stats (lines 415/435/449) still call O(n) `calculateStatistics`. Incremental methods exist (lines 552-623) but are unused.
- Trigger comment (id 4212107089) was integrity-filtered — could not determine if explicit review was requested.

## Open Quality Questions (never resolved)
1. Correctness test: no test coverage proposed for stat drift on `cachedStatistics == nil` fallback path
2. WAL correctness: `pendingWrites` must contain the new entry BEFORE `appendToWAL` is called — this holds (line 180 appends first), but a unit test would lock it in
3. `removeAll` still calls `calculateStatistics(for: [])` — acceptable (early return for empty array per issue body)

## Resolution
NOOP — issue closed, code changes not applied, comments integrity-filtered.
