# #935 remaining acceptance plan: physical route-disruption controlled stop

Decision: NARROW to hardware qualification. The controlled-stop implementation landed through #1032/main f8af0b11. No new engine-recovery architecture or duplicate implementation PR. Read reviews/935.json, current issue decision and actual installed Alpha source. Use the next existing device/Alpha opportunity; do not dispatch a release.

## Confirm software baseline

Current CaptureDisruptionObserver filters the owned engine, hops to MainActor and checks capture generation/usability. Apple, OpenAI and SharedClient live capture wire their own observers; hands-free owns its separate detector session. TranscriptionRecordingService routes disruption through normal bounded finalisation and preserves originating destination/keyboard ownership. Automatic continuation is deferred: this issue expects a truthful stopped state and fresh capture after the route settles.

Run existing Apple-native CaptureDisruptionObserverTests, HandsFreeCaptureDisruptionTests, interruption/loss/owner lifecycle suites and iOS build. Retain duplicate notification, other-engine, queued stale callback, usable route and active-utterance finalisation cases. Do not infer actual Bluetooth state or buffer cessation from simulated notifications.

## Physical matrix and evidence

Use a physical iPhone and available Bluetooth headset/route device with existing authorised credentials. Record iPhone/iOS, headset model/firmware where visible, app train/version/build/source, model/provider and trigger/destination. Use synthetic identifiable phrases immediately before/during the transition. Observe current input route and actual capture/tap/level cessation using existing diagnostics; a route reason by itself does not establish microphone loss. No new telemetry or private audio logging.

Cover Apple live capture, OpenAI Realtime and one shared backend. For each, start a real recording then connect/disconnect the relevant Bluetooth route or reproduce an actual format/engine disruption. Confirm exactly one controlled finalisation, UI/shared state no longer claiming capture, retained pre-disruption words and existing audio, normal destination semantics and no duplicate output. If finalisation fails, distinguish recovered partial text from actual persistence failure; do not require words after input ceased to appear.

Exercise destinations across the matrix: default/clipboard, History Only and a keyboard-owned request. History Only must not copy unexpectedly; keyboard completion must retain its originating request ownership. Verify #934 raw-once clipboard behaviour remains unchanged. After the route settles, a fresh recording succeeds and queued notifications from the old run cannot terminate it.

Hands-free needs two distinct checks: (1) armed detector with an input-preserving notification remains armed; (2) stopped/unusable detector or actual input loss disarms truthfully. During an active utterance, preserve/finalise that utterance before disarming. Trigger duplicate route/configuration events and manual Stop close together where practical; observe a single result. Idle/other-engine changes must not produce a recording error. Do not implement automatic resumption to hide a stopped detector.

Attach a concise per-run table of actual route change, whether capture became unusable, final result/destination, retained recording, fresh-start result and source/build. Explicitly mark untriggered hardware cases unavailable. Keep call/Siri interruption qualification in #936/#946 and idle keyboard readiness in #995; they are not scope expansion here.

If actual controlled-stop behaviour fails, preserve the smallest deterministic reproduction and propose only a targeted fix, reusing existing observer/lifecycle seams and tests. Otherwise close only the remaining device acceptance with evidence in the existing issue/owner record. Device absence means hardware gate stays open; Linux, simulator notifications and merged code are supporting evidence only. No settings/category/provider/runtime/container changes, purchases or external posts are authorised by this plan.
