# Issue #246 — [Perf Improver] incremental transcript append in DeepgramLiveController.handleTranscript

**Status:** quality-approved (re-confirmed 2026-04-09)
**Labels (at re-confirmation):** planning:in-discussion, planning:needs-product, planning:needs-security, planning:needs-performance, planning:architecture-approved, planning:design-approved, planning:reliability-approved, planning:quality-approved
**Date:** 2026-04-09

## Summary
Replace O(N) `finalSegments.map(\.text).joined()` with incremental append in `DeepgramLiveController.handleTranscript` (line 653).

## Code verified
- Line 653: O(N) hot path in Deepgram — confirmed fix is straightforward.
- Line 1195: O(N) in AssemblyAI (replace+append branching) — OUT OF SCOPE for this issue. Files section = Deepgram only.
- Line 948 `buildFinalResult()`: one-shot at session end — not a hot path, leave alone.

## Quality approval rationale
1. **Scope** resolved: Files section anchors scope to Deepgram only. AssemblyAI is optional/secondary and excluded.
2. **Architecture** approved (was last dependency).
3. **Correctness test** — PR-stage non-negotiable guardrail: the test must include `XCTAssertEqual(fullTranscript, "word1 word2 ... wordN")` alongside `measure {}`.

## PR-stage guardrail (non-negotiable)
- `measure {}` block alone is not sufficient. Require at least one correctness assertion: after N appends, `fullTranscript == expected_joined_text`.
- Failure mode if skipped: subtle off-by-one (space handling) would survive benchmark and ship silently.
- If AssemblyAI is included in the PR (not planned), it must have separate correctness verification for the replace path.

## Notes
- Labels were inconsistent with memory on 2026-04-09 (needs-quality still present despite prior approval). Re-applied quality-approved on this pass.
