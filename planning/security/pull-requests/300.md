# PR #300 — chore(agentic-workflows): sync improved planning flow

**Date reviewed:** 2026-04-11
**Status:** No action — PR already merged when trigger arrived

## Summary
Syncs agentic planning, review, and improver workflow surface from career-framework. Adds helper workflows (issue-ready-to-pr, retry/autostart dispatchers, agentic-improvement config), upgrades gh-aw from v0.62.3 to v0.67.4, adds Reliability/EM/Design roles, new `start_issue_planning.py` script. All changes are workflow automation only.

## Trigger context
Triggered by `issue_comment` (comment-id 4228888078) from `coderabbitai[bot]` — a rate-limit warning, not a plan-review comment.

## Labels at time of review
None. No `plan-review:` labels, no `### 🔎 Plan Review Kickoff` comment.

## Decision
PR was already merged (merged_at: 2026-04-11T08:33:59Z) when this run started. Instruction: "If this run came from issue_comment and the pull request is closed or merged, do nothing." Called noop.

## Security notes (for reference only)
- All changes are `.github/workflows/` and `.github/scripts/` — automation surface only, no product attack surface.
- `permissions: {}` top-level with least-privilege job overrides maintained in updated workflows.
- `start_issue_planning.py` uses `gh` CLI (authenticated via Actions token) to manage labels and comments — no new credentials surface.
- gh-aw version bump v0.62.3 to v0.67.4: action SHAs are pinned in manifests (security-positive).
- Activation condition in `issue-planning-security.lock.yml` now checks `planning:needs-security` label before firing — scope-narrowing improvement consistent with prior principles.
- No user data, secrets, or trust boundary changes.
