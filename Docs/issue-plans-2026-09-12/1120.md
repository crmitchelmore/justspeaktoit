# #1120 implementation plan: isolate local-transcription benchmarks

Decision: IMPLEMENT; SOL medium. Independent of provider folds, settings and #1121 runtime decisions. Move the experimental binary dependency out of the shipping root package without deleting benchmark qualification or the public transcribeCpp engine mapping. Read AGENTS.md, reviews/1120.json and current origin/main before creating an isolated worktree. Coordinate Package.swift/ManifestParity edits with concurrent native fixes.

## Manifest and file moves

Create `Benchmarks/LocalTranscription/Package.swift` with Swift tools 5.9, macOS14, the existing `local-transcription-benchmark` executable product, `LocalTranscriptionBenchmarkKit`, executable and test targets. Depend on the root `SpeakCore` product through `../..`; use an explicit stable package dependency name/product reference so a differently named worktree directory cannot break local package identity. Add the existing argmax-oss-swift requirement providing WhisperKit (`from: 1.1.0` at inspected base) and preserve the currently resolved runtime version. Do not upgrade WhisperKit or any other dependency in this move. The executable imports SpeakCore directly, so declare that product dependency explicitly as needed rather than rely on a transitive import accident.

Move the CTranscribe binary declaration verbatim, including the split URL for transcribe.cpp v0.1.3 and checksum `b7a3442e2f3552cac1ee71b5e164934dd4db243f6b4b16b1e3e3ed5d1645eefd`. No new binary source, checksum or C shim changes. Move these directories using tracked renames:

- `Sources/LocalTranscriptionBenchmark` → `Benchmarks/LocalTranscription/Sources/LocalTranscriptionBenchmark`.
- `Sources/LocalTranscriptionBenchmarkKit` → `Benchmarks/LocalTranscription/Sources/LocalTranscriptionBenchmarkKit`.
- `Tests/LocalTranscriptionBenchmarkTests` → `Benchmarks/LocalTranscription/Tests/LocalTranscriptionBenchmarkTests`.

The destination already contains corpus.example.json, evidence.example.json and results; preserve those bytes/paths. Preserve runner resource accounting, models/backend CLI values, corpus validation, error-rate metrics and assessment gates. Do not rerun or overwrite historic benchmark results as part of a mechanical move.

Remove only root CTranscribe, the two benchmark targets/test target and executable product from `Package.swift`. Keep the root argmax dependency and SpeakApp WhisperKit/FluidAudio links; they are shipping dependencies. Leave Tuist's native graphs, supported products and ModelRouting enum unchanged. `LocalTranscriptionEngine.transcribeCpp` and both `transcribe.cpp`/`transcribe-cpp` forms have nonbenchmark consumers/tests (`ModelCatalogTests`); removing them would be an unnecessary public API break.

Resolve root and benchmark independently on Apple Swift. Commit the benchmark's own Package.resolved because its runtime-version evidence must describe the graph it runs. Regenerate root Package.resolved only as required by actual resolution, avoiding unrelated pin churn; removal of a binary target does not imply a remote package pin must change. Do not hand-edit resolved versions to make tests pass. A path dependency on SpeakCore may still cause SwiftPM to resolve some root remote package requirements; this issue guarantees removal of CTranscribe from root ownership, not complete isolation of every transitive resolver cost.

## Preserve checksum and runtime provenance

Update `scripts/verify-checksums.sh` to read the benchmark manifest and report that exact path in extraction errors. Keep multiline URL concatenation, checksum format validation, curl failure handling, compute-checksum comparison, temp cleanup and the current lint-baseline ceiling. Make package resolution explicit for the benchmark (`swift package --package-path Benchmarks/LocalTranscription resolve`) before its artifact verification; root resolution can remain a separate existing validation if preserving script behaviour requires it. Do not let a missing target be treated as success. Keep `make verify-checksums` as the existing command, with the unchanged artifact checked.

`Tests/SpeakCoreTests/ManifestParityTests.testArgmaxPin_matchesTheBenchmarkRuntimeVersion` currently compares root Package.resolved to the soon-to-move runner. Move this benchmark-specific test into the benchmark test target, or a focused new `BenchmarkManifestParityTests.swift` there. Calculate package root from its new #filePath, read benchmark Package.resolved and moved EngineRunners.swift, and assert the reported argmax runtime string matches the actual benchmark pin. Keep the separate root `testRemotePackageRequirements_matchBetweenPackageSwiftAndProjectSwift` exactly meaningful for shipping graphs. Do not retain a root test that requires benchmark resolution/artifact download just to run app tests, and do not delete version provenance coverage. Recheck the benchmark's explicit requirement remains compatible with the root local dependency; a small manifest requirement assertion is useful if resolution otherwise gives an obscure conflict.

## Commands, docs and CI

Add `.PHONY: bench` and `bench` help entry to Makefile invoking `swift run --package-path Benchmarks/LocalTranscription local-transcription-benchmark $(BENCH_ARGS)` from repository root. Provide a documented `BENCH_ARGS` pass-through for existing `run` and `compare` subcommands; no parser redesign. The current parser has usage output on missing/invalid arguments, not an implemented --help flag. Verify that behaviour honestly, or add a tiny conventional --help path only if necessary for the new documented invocation. Do not silently make `make bench` download models or execute a real corpus by default.

Update current invocation examples in `Docs/TRANSCRIBE_CPP_ASSESSMENT.md` around build/run/compare to include the package path or `make bench BENCH_ARGS='...'`; preserve repository-relative manifest/result paths because ArgumentParser resolves them from current working directory. Keep historical test/result claims labelled as historical rather than rewriting them into new evidence. Search README, scripts, Makefile, docs and workflow files for old paths/commands. No benchmark-named workflow was found at inspected base, so relocating tests would otherwise remove default CI coverage.

Add an explicit native benchmark validation path, preferably a small path-filtered macOS workflow/job plus workflow_dispatch, that resolves/builds/tests this package. It must run the three existing suites `AssessmentGateTests`, `TranscriptErrorRateTests` and `BenchmarkCorpusTests`, and the relocated runtime-pin invariant without running model inference. Include benchmark sources/manifests/lockfile and relevant SpeakCore/root-manifest/checksum changes in path filters. Existing shipping required checks remain unchanged; do not make every app build resolve the benchmark again. If repository policy favours an existing workflow job, use conditional changes detection instead of requiring this heavy binary download in every root job. Record its actual job name and triggers in the PR.

## Verification and finish criteria

Static checks: diff/rename audit; root dump-package has no CTranscribe or benchmark target/product; benchmark dump-package contains the expected targets and root SpeakCore product dependency; only moved runner imports CTranscribe; current shipping targets/dependencies unchanged. Keep core transcribeCpp/legacy-string decoding tests. Check all original benchmark files and tests are accounted for and no result JSON changed.

On Apple CI use fresh, task-specific build/scratch directories (not deleting another worker's shared .build): resolve/build root and inspect that root artifact tree contains no CTranscribe/TranscribeCpp archive or extraction. A dirty existing artifact directory is not evidence that the new root graph downloads it. Separately resolve/build/test the benchmark package; run `make verify-checksums` and exercise `make bench` usage plus a deterministic compare command using synthetic valid reports if already supported by fixtures. No microphone, model downloads or costly inference is required for packaging acceptance. Native lint and normal root tests/build/iOS CI must still pass; API compatibility should show no public engine removal.

Document exact commands/toolchain and whether the checksum download/native gates completed. Linux manifest inspection cannot establish XCFramework linkage, and no quantified build-time savings should be claimed without measurement. Submit a scoped normal PR, preserving current assessment conclusions and keeping #1121 runtime retention/removal entirely separate. This planning artifact is not a Beads tracker update.
