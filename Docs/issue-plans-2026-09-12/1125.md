# #1125 implementation plan: remove obsolete maintenance entry points

Decision: IMPLEMENT; SOL medium. Small independent cleanup. Use current origin/main and an isolated worktree; coordinate README/DistributionBuildIdentityTests changes with active release work. Read reviews/1125.json and AGENTS.md. Preserve every active Alpha/Stable publication gate. No app changes, release dispatch, protection edits or local untracked-directory cleanup.

## Exact patch

Delete the three obsolete scripts: `scripts/bump-version.sh`, `scripts/version.sh`, `scripts/verify-binary.sh`. Repeat tracked/hidden-file reference search first, excluding .git and generated dependency caches; at reviewed base only README references version.sh, and the other two have no external callers. Do not delete `scripts/release-train.mjs`, checksum validation, actual binary/archive validation or current build-number utilities based on similar names.

Delete retired workflow stubs `.github/workflows/auto-release.yml` and `.github/workflows/publish-speak-cli.yml`. Both are manual retired surfaces; current Stable/Alpha and CLI publication live elsewhere. Delete the advisory `.github/workflows/conformance-pr-review.yml` together with its candidate/unratified root `patterns.anchors.yaml`, `patterns.config.yaml`, `patterns.map.yaml`, subject to the concrete protection gate below. The conformance workflow is active advisory external CI, not literally dead; describe its removal accurately. Do not leave an external action enabled with its configuration files removed.

Replace only README's obsolete Versioning section. It currently claims BUILD and recommends version.sh bump commands. State that VERSION is a repository hint, current build allocation/manifests are managed by release-train.mjs and active workflows, and link `Docs/alpha-stable-release-trains.md` plus the iOS runbook where relevant. Preserve the distinction between automatic Alpha delivery and owner-approved Stable publication. Do not replace old examples with commands that allocate, tag, promote or publish when a contributor merely wants to build. The runbook is canonical for current version/build mechanisms; do not casually substitute BUILD_NUMBER for BUILD without checking actual train allocation ownership.

In `Tests/SpeakAppTests/DistributionBuildIdentityTests.swift`, update two concrete stale file reads:

- `testIOSReleaseWorkflowSignsAndValidatesKeyboardExtension` reads auto-release.yml and asserts it cannot pass include_keyboard=true. Replace that retired-file read/assertion with a file-absence assertion, while retaining every active release-ios.yml keyboard profile, entitlement/App Group, iCloud container, direct-capture and immutable-manifest assertion.
- The frozen-candidate/CLI publication test near the old publish-speak-cli.yml read currently inspects a retired stub. Replace with absence assertion; keep candidate artifact hash/checksum, exact approved manifest, appcast, Homebrew and actual CLI publication assertions intact.

Search all tests for the deleted names and preserve meaningful active safeguards. Negative absence tests may intentionally mention deleted paths. A single focused absence assertion for obsolete entry points is enough; do not add a large mirror-of-filesystem test suite. Remove stale documentation links if discovered. AGENTS.md changes here should be only stale references; #1129 owns its Project Structure link update. Do not delete untracked `SpeakiOS.xcodeproj` or another worker's generated project: it is not a tracked PR deliverable and removing it offers no accepted user benefit.

## Conformance workflow protection gate

Parent obtained read-only GitHub evidence on12 September2026: repository rulesets expose one active main ruleset20875321. Its required checks are Build & Test(macOS), BuildiOSLibrary and Lint (strict), with no conformance context. Evidence: [main ruleset](https://github.com/crmitchelmore/justspeaktoit/rules/20875321). The separate legacy branch-protection endpoint returned403 ResourceNotAccessibleByIntegration, so that configuration family was not verified. Do not claim all protection is known or modify protection to accommodate this cleanup.

Prepare the full reviewable patch and tests. Before merging the advisory-workflow deletion, obtain a permitted read-only confirmation of any legacy required context or inspect the PR's required-check evaluation. If a conformance context is required, preserve the advisory workflow plus its three profiles as a coherent deferred unit and land the remaining cleanup; do not remove a required gate or rename it. If the missing protection visibility cannot be resolved, keep the full deletion as a draft with that explicit final merge gate, or split off the known-safe seven-file/script/README/test cleanup. No need to ask the user to approve ordinary reversible editing; parent handles final integration decisions. The known active ruleset already supports the intended deletion, but a403 is not evidence of absence.

## Verification and handoff

Run release-train Node tests using the current package scripts/test runner (inspect scripts/tests and package.json), `git diff --check`, hidden/tracked reference and Markdown-link checks. Run the targeted DistributionBuildIdentityTests on Apple Swift CI and normal lint/required CI. Linux can validate scripts/docs and static test changes, not native XCTest compilation. No microphone/device gate is required for this maintenance-only change.

Review the final diff to ensure `.github/workflows/prepare-stable.yml`, publish-stable.yml, active release-mac/release-ios/Mac-App-Store/Alpha workflows, release-train.mjs and Config/ReleasePipeline.json are unchanged. Explicit reviewed manifest SHA-256 approval, pristine-source release tests before manifest stamping, keyboard signing and frozen candidate asset checks must remain. Existing release-train tests must pass; do not weaken them to remove a failing guard.

Submit a scoped conventional PR explaining obsolete entry points, corrected documentation, preserved gates and exact protection evidence/limits. Do not describe this as a product feature or a distribution success. Do not post externally or dispatch releases under this plan. Beads is unavailable; record analysis/test evidence honestly rather than claiming tracker updates.
