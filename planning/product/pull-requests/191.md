# PR #191 — fix(agentic-workflows): disable failure issue creation on all workflows

## Summary
Changes `GH_AW_FAILURE_REPORT_AS_ISSUE: "true"` → `false` in all 31 workflow `.lock.yml` files.
Stops `[aw] ... failed` issues flooding the tracker. Failures remain visible in Actions tab.
Zero user-facing impact.

## Linked plan issue
None found in PR body. No `Plan issue:`, `Closes #`, `Fixes #`, or `Refs #`.

## Product stance
**Blocking (missing plan link).**
The change is clearly scoped and non-controversial from a product lens.
Path to yes: add a `Plan issue: #<n>` reference (new or existing issue) to the PR body.

## Open concerns
- No linked planning issue → cannot approve per review protocol

## Status
`needs-product` (as of 2026-03-26)
