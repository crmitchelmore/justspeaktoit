# Issue #283 — fix(ios): add missing SpeakCore import

**Status**: Approved

## Summary
One-line compile fix: add `import SpeakCore` to `SpeakiOSApp/SpeakiOSApp.swift`.
TestFlight releases have been broken since March 26 due to missing import.

## Reliability assessment
- **Blast radius**: Zero — import statement has no runtime effect
- **Rollback**: N/A; compile fix can't regress runtime behaviour. If it somehow did, TestFlight staged rollout + App Store review gate means no immediate user impact
- **Failure mode**: Current failure IS the missing import (build fails). Fix can only improve state
- **Monitoring**: No new surface; no Sentry changes needed
- **Fix confirmed correct**: `OpenClawClient` is defined in `Sources/SpeakCore/OpenClawClient.swift`; `SpeakiOSLib` has no `@_exported import SpeakCore`

## Decision
Approved immediately. No reliability concerns on a compile-time fix unblocking a broken pipeline.
