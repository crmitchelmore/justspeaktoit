# Issue #162 — Add plan-linked PR review after issue planning

## Latest stance
**Approved** as of 2026-03-25.

## Summary
Adds a new PR review stage: when a PR body contains `Plan issue: #<n>` pointing to a `planning:ready-for-dev` issue, all five roles review the PR against the approved plan, diff, and verification evidence. Reaches `ready-to-merge` only when all five roles approve.

## Resolved by maintainer synthesis (comment 2026-03-25T08:23:24Z)
1. PR body syntax: `Plan issue: #<number>` with HTML comment placeholder so stale placeholders can't look valid.
2. Linked-issue validation: must be `planning:ready-for-dev`; missing/placeholder/unapproved = blocker.
3. Label namespace: `plan-review:*` (isolated from `planning:*`).
4. Manual dispatch: scoped to explicit PR number; no accidental reconciliation.
5. Memory: `pull-requests/<pr-number>.md` per role, separate from `issues/<n>.md`.

## Cross-role status at approval
- Security: approved
- Performance: approved
- Architecture: approved
- Code Quality: needs-quality still pending (maintainer synthesis likely resolves it)
- Product: approved

## Product guardrails (non-blocking)
- `ready-to-merge` must be automation-only assignable (Security flagged; confirmed by plan).
- Portability documentation is in scope — confirm it appears before closing the PR.
