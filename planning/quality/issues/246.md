# Issue #246 — [Perf Improver] incremental transcript append in DeepgramLiveController.handleTranscript

**Status:** needs-quality — open questions
**Labels as seen:** planning:in-discussion, planning:needs-quality, planning:reliability-approved (+ needs-product/security/performance/architecture/design)
**Date:** 2026-04-07

## Summary
Automated perf issue. Replace O(N) `finalSegments.map(\.text).joined()` with incremental append in `DeepgramLiveController.handleTranscript` (line 653). Same pattern noted in `AssemblyAILiveController` (line 1195) but plan scope is ambiguous.

## Code verified
- Line 653: confirmed O(N) hot path in Deepgram.
- Line 1195: confirmed O(N) in AssemblyAI, but AssemblyAI has replace+append branching (lines 1186-1193).
- Line 948 `buildFinalResult()`: map+join called once at session end — NOT a hot path. Leave alone.
- Reset paths (lines 860-862, 941-943) are untouched by the fix.

## Open quality concerns
1. **Scope**: Files section scopes to Deepgram only. AssemblyAI mentioned as "can be handled" — in or out?
2. **Correctness test**: `measure {}` block proves performance but not correctness. Need one assertion that transcript text == expected after appends.

## Failure mode named
For AssemblyAI: incremental cache diverges from `finalSegments.map(\.text).joined()` on the replace path. Silent wrong transcript is the failure mode.

## Cross-role notes
No cross-role convergence to report yet; most roles haven't weighed in.
