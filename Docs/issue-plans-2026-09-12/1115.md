# #1115: extract iOS settings without behaviour changes

IMPLEMENT with SOL medium, independently of #1104. Sequence this before #1116/#1117 to avoid overlapping settings edits. Read AGENTS.md, README, design guidance and review 1115. Use an isolated branch/worktree; parent owns integration. Beads is absent. This is mechanical ownership/key-safety work, not a new settings architecture, UI redesign, key migration or runtime performance improvement.

## Corrections to issue prose

Current `Sources/SpeakiOS/Views/SettingsView.swift` has 3,159 lines, not a fixed 3,178. Locate symbols/braces rather than blindly cutting issue line ranges. `AppSettings` starts around 86 and ends around 998. There are **17** locally owned persisted keys: 16 literal forKey values, including hasLaunchedBefore, plus rememberedRemoteTranscriptionMode. Do not write the issue's incorrect count-16 acceptance test.

The adjacent `LiveModelGroup` and `BatchModelGroup` are UI grouping helpers, not AppSettings internals. `IOSTranscriptionLocation` is used by settings selection APIs as well as views. Existing file/type lint suppressions exist, while the current baseline has **zero entries for this file**; do not invent/migrate nonexistent baseline entries or broadly regenerate it.

## 1. Mechanical AppSettings extraction

Create `Sources/SpeakiOS/Settings/AppSettings.swift`, retaining `#if os(iOS)`/`#endif`. Move the complete `@MainActor public final class AppSettings: ObservableObject`, preserving all declarations, access modifiers, default values, init order, didSet side effects, comments, tasks, actor isolation and notification cleanup. Preserve `public static let shared`, public/read-only properties and the existing internal injectable initializer `init(defaults:loadsSecureStorage:credentialStorage:)` exactly.

The class needs Foundation, Combine, SpeakCore, SpeakSync, Security and OSLog; inspect actual symbols before adding SwiftUI/UIKit imports. `ObservableObject`/Published come from Combine. Keep required imports for UIApplication/protected-data notifications if present. Do not remove an import solely because Linux's platform guard skips it.

Move `HardwareTriggerDestination`, `IOSTranscriptionMode`, and `IOSTranscriptionLocation` unchanged into the same settings-owned file, or a focused `Settings/SettingsTypes.swift` if that makes the class file simpler. Preserve public visibility of the first two, internal visibility of the third, every raw value/display label and destination semantics. Never restore removed Send to Mac destinations. These types support non-view consumers and must not remain stranded in the old monolithic view.

Keep all secure-storage identifiers and shared keys in their canonical owners: e.g. `LiveTranscriptionSelection.DefaultsKey`, keychain IDs/configuration, app-group keyboard profile publishing and CloudKit sync. Do not fold them into a local defaults enum or replace them with guessed strings. This issue does not change keychain defaults, protection, sync or first-launch provider selection.

## 2. Exact local DefaultsKey mapping

Add nested internal `enum DefaultsKey: String, CaseIterable` in AppSettings, with the following case names/raw strings (implicit raw values are safe because names match):

| Cases/raw persisted strings |
|---|
| selectedModel, transcriptionMode, rememberedRemoteTranscriptionMode |
| batchTranscriptionModel, transcriptionKeywords |
| liveActivitiesEnabled, visualDensity, autoStartRecording, handsFreeDictationEnabled |
| preferredLocale, hardwareTriggerDestination |
| autoStopOnSilenceEnabled, autoStopSilenceSeconds |
| postProcessingEnabled, postProcessingModel, autoPostProcess, hasLaunchedBefore |

Replace every class-local literal defaults read/write with `DefaultsKey.<case>.rawValue`. Replace every `Self.rememberedRemoteModeKey` use with `DefaultsKey.rememberedRemoteTranscriptionMode.rawValue` and remove that internal static after repository reference search. Do not rename preferredLocale to preferredLocaleIdentifier: the latter is a property name, the former the persisted contract.

Do not alter bool-versus-object read semantics or nil fallbacks. In particular retain first-launch hasLaunchedBefore, true default for liveActivitiesEnabled, remembered remote-mode restoration, silence-window object lookup/default/clamping, selected-model normalisation and publication to the keyboard profile. No migration is necessary because storage names remain identical.

## 3. Move views by responsibility, preserving bodies

Keep the public root `SettingsView` at `Sources/SpeakiOS/Views/SettingsView.swift` to minimise reference churn. It must end under 800 lines. Preserve its full body, state, tasks, modifiers, root preview, and the private SettingsView extension containing selection bindings/missing-key presentation. Current root is about 720 lines; keeping that roughly 37-line extension with it fits after helpers move.

Create files under `Sources/SpeakiOS/Views/Settings/`:

- `HardwareTriggerSettingsView.swift`: that view plus its private StepRow/BulletRow if only it uses them.
- `PostProcessingSettingsView.swift`: matching view.
- `APIKeysView.swift`: matching view and helpers private to it; preserve every key field/action/validation state.
- `PrivacyView.swift`: matching view plus its preview and PrivacyWorkflowRow if exclusive.
- `CloudKitSyncSettingsSections.swift`: CloudKitSyncSettingsSection and CloudKitKeySyncSettingsSection with necessary SpeakSync imports.
- `SettingsRowHelpers.swift`: FeatureRow, InfoRow, CheckRow, PermissionRow and actually shared row helpers.
- `TranscriptionModelGroups.swift`: LiveModelGroup/BatchModelGroup unchanged; use internal visibility only where multiple moved views need them.
- `MissingTranscriptionAPIKeyAlert.swift`: IOSMissingTranscriptionAPIKeyAlert and the View alert modifier, with minimum internal access to allow root/API-key consumers. Preserve all text/actions and state-reset behaviour.

Inspect usage before widening private. A private root extension stays with its type; helpers used across files become internal, never public just to compile. Keep same-type private members private. Preserve iOS guards in every new file and SwiftUI/other imports actually required there. Do not rewrite view bodies, grouping catalogues, labels, navigation, bindings, permissions, colours or ordering. Specifically do not use extraction to fix BatchModelGroup's local provider-name dictionary; that is separately reviewed catalogue work.

Package.swift includes `Sources/SpeakiOS`; Tuist's iOS app consumes that library. No source-list or generated-project edits should be necessary. Confirm with Tuist generation; only change manifests if actual discovery fails, and explain why.

## 4. Verification and minimal tests

Add `Tests/SpeakiOSTests/AppSettingsDefaultsKeyTests.swift` under the same iOS guard/@testable import. One compatibility test asserts the exact set of 17 raw values above, with uniqueness, not count alone. This guards the real persistence contract rather than mirroring an implementation detail. Existing behavioural persistence tests must remain unchanged and pass; do not modify expectations to accommodate accidental behaviour drift.

Retain and run `TranscriptionSelectionPersistenceTests` (Soniox relaunch, local/remote memory, restored batch mode, representative settings), `HardwareTriggerDestinationTests`, `HandsFreeDictationSettingsTests`, `OpenRouterAudioSettingsTests` and `CredentialLoadingTests`. Existing injected UserDefaults suites must stay isolated; do not run tests against real user defaults/keychain. No extra test framework or speculative tests needed.

Static checks: zero `class AppSettings` in root; exactly one class definition under iOS Settings; root under 800 lines; no literal `forKey: "` inside extracted class; no rememberedRemoteModeKey references; same local legacy-key set; diff movement/word diff shows only import/access/key-expression changes. `git diff --check` must pass. Diff each moved view declaration/body against the old source to catch omitted trailing extensions/previews and accidental duplicate definitions.

Lint: run make lint. Fix trivial import/whitespace issues only. Remove obsolete file_length suppression from split view files when unnecessary; do not add new broad disables. Preserve existing targeted suppressions where they still apply. If extraction exposes legitimate class file/type length violations, record only precise new-path baseline entries using the repository's JSON baseline format/tools; never regenerate unrelated entries. Current old-file baseline is empty, so no blanket path migration is warranted.

Apple Swift/Xcode is required: `make lint`, appropriate package build, `tuist generate`, then existing ci.yml SpeakiOS build-for-testing/test commands and SpeakiOSTests; run exported API compatibility check because public iOS types moved. Pure filesystem/Swift-on-Linux checks do not prove actor/SwiftUI/Security compilation. No microphone/provider gate is needed for this mechanical change. A brief simulator navigation smoke through root/hardware/API keys/privacy/sync catches missing view wiring; preserve settings across relaunch using test data. Report actual results and limitations honestly. Keep normal PR/review/CI gates and no release dispatch.
