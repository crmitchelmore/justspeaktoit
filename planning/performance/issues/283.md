# Issue #283 — fix(ios): add missing SpeakCore import

## Stance: APPROVED

## Summary
One-line compile fix: add `import SpeakCore` to `SpeakiOSApp/SpeakiOSApp.swift`.
Unblocks iOS TestFlight releases broken since March 26.

## Performance assessment
Import declarations are resolved at compile time. Zero runtime overhead.
Hot path (audio, transcription, HUD) is entirely unaffected.
Nothing to measure; no latency, CPU, or memory budget changes.

## Approved: yes — no performance concerns
