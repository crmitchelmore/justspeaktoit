# PR #186 — feat(agentic-workflows): AgenTek planning team improvements

**Date:** 2026-03-26
**Status:** Not yet approved — missing linked plan issue

## What the PR does
- Adds two new planning roles: Sam Chen (EM facilitator), Jordan Park (Reliability/DevOps)
- Enriches 7 existing agent personas with names, cross-role dynamics, `team-dynamics.md` inter-agent memory
- Adds operational workflows: synthesis (on ready), memory curator (bi-weekly), repository context refresh (weekly), stale issue cleanup (weekly), improvement coordinator
- Reconcile updated to require 6 approvals (was 5); dispatches synthesis on ready
- `agentics-maintenance.yml` cron changed from every 6h to every 2h

## Security observations
- **No `Plan issue:` reference in PR body.** Primary blocker.
- Permissions pattern is consistent with established pattern: `{}` top-level, agent job `contents/issues/pull-requests: read`, conclusion job `contents/discussions/issues/pull-requests: write`.
- Fork guard (`head.repo.id == repository_id`) preserved in all PR-triggered workflows.
- `stale-issue-cleanup`: auto-closes up to 20 issues/run when labelled `agentic-workflows` and no human engagement. Guardrails look reasonable. Irreversible action but scoped correctly.
- Improvement coordinator: creates 1 issue max/run with `automation, coordination` labels. Read-only beyond that.
- `.lock.yml` `${{ }}` wrapping change in `if:` condition is valid GitHub Actions syntax, no behavior change.
- New synthesis workflow dispatch in reconcile is wrapped in try/except; doesn't block ready-for-dev.

## Blockers
1. No linked planning issue → cannot verify implementation matches an approved plan.

## Open questions for maintainer
- Add `Plan issue: #<n>` to PR body. Which issue covers this work?
