# PR #191 — fix(agentic-workflows): disable failure issue creation on all workflows

**Branch:** `chore/silence-failure-issues`
**State:** open, not draft
**Labels:** plan-review:in-discussion, needs-* (all roles)

## Summary
Adds `report-failure-as-issue: false` to all 31 workflow safe-outputs blocks.
All changes are `.github/`-scoped — no app code.

## Architecture stance
**Blocked — no linked planning issue.**

PR body contains no `Plan issue: #<n>`, no `Closes/Fixes/Resolves/Refs` keyword.
Per review protocol, cannot approve without a clear linked plan issue that is `planning:ready-for-dev`.

The structural change itself is low-risk and clean: single flag added symmetrically across all workflows.
If a planning issue is linked and approved, no architecture objections to the diff.

## Open blockers
1. No linked planning issue.

## First comment left: yes (2026-03-26)
