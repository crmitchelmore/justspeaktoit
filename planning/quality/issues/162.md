# Issue #162 — Add plan-linked PR review after issue planning

## Latest stance: APPROVED

## My four gaps from the first round
1. PR template field format undefined → CLOSED: `Plan issue: #<number>` with HTML comment placeholder
2. Linked-issue parsing/validation → CLOSED: missing/placeholder/not-ready are all hard blockers
3. Label namespace collision → CLOSED: `plan-review:*` namespace separate from `planning:*`
4. Manual dispatch isolation → CLOSED: auto-run only on `pull_request`/`issue_comment`; manual dispatch requires explicit PR number

## Resolved by
Maintainer comment at 2026-03-25T08:23:24Z directly answering all four questions.

## Notes
- Product still had `planning:needs-product` at time of approval; this issue can only move to `ready-for-dev` once Product approves.
- Role memory for PR review should live in `pull-requests/<pr-number>.md`.
- Connected PR is #161.
