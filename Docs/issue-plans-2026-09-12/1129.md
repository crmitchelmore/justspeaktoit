# #1129 implementation plan: accurate current-state architecture reference

Decision: NARROW; SOL medium. Rewrite `Docs/Architecture.md` below400 lines and replace duplicated AGENTS.md structure inventory with a link. This can ship now: #1113/#1117 plans are not landed architecture. No app/manifest changes, future-state framework diagram or native/device claims. Read reviews/1129.json and current origin/main, including Compare Models added by main2ff982d. Coordinate #1125 README/obsolete patterns cleanup and #1120 benchmark relocation.

## Build the factual inventory first

Enumerate every declared owned target/product in current `Package.swift` and all Tuist targets in `Project.swift`, including conditional/test/demo/binary targets and auxiliary local packages. Use manifest parsing/dump-package where available, corroborated by source; do not infer graph edges from similar names. Record actual dependencies versus direct shared source inclusions. At inspected base the root includes SpeakCore, SpeakSync, SpeakHotKeys, SpeakiOSLib, SpeakAutomationKit, SpeakCLI, SpeakApp, SpeakHotKeysDemo and their tests, plus benchmark kit/executable/tests and CTranscribe. If #1120 is merged, inventory the benchmark under its separate manifest and show that CTranscribe is absent from shipping root ownership. Tooling is a separate dependency graph, not another app module.

Tuist declares SpeakApp, SpeakiOS, JustSpeakWatchApp, JustSpeakWatchWidgetExtension, JustSpeakKeyboard, JustSpeakShare, JustSpeakToItWidgetExtension, CoreJourneyFixtureApp, SpeakAppUITests, SpeakiOSUITests and SpeakiOSTests. Record actual feature flags for Watch/keyboard/direct capture/share and deployment targets from manifest; generated projects are not hand-maintained module declarations. If #1123 has not merged, Watch directly compiles selected core files, which is not a package dependency. If it has merged, describe the actual successful package layout, not its original recipe.

Use a compact target table to cover the full inventory. Small scoped Mermaid graphs should explain runtime/build relationships without a giant node-per-test hairball; distinguish module dependency arrows from data transfer. Limit horizontal density and link source files in adjoining prose. A table may group test targets only if every exact name remains visible. Keep total document roughly250–350 lines, below400.

## Document sections and required corrections

1. **Module/build map:** Package.swift, Project.swift and Workspace.swift ownership, generated Xcode workspace/projects and separate Tooling/benchmark graphs as landed. Explain app versus library target identities without promising all Apple platforms compile from host SwiftPM.
2. **Shared responsibilities:** catalogues/model routing/provider clients, capture lifecycle/preroll, keychain/types, keyboard/App Group protocols, release train values and history presentation. Name exact files: `Sources/SpeakCore/RecordingStartSequence.swift` contains RecordingStartSequencer; StreamingAudioPreroll.swift and PersonalLexiconService.swift are core. `Sources/SpeakCore/TranscriptionProviderRegistry.swift` owns protocol/metadata, while `Sources/SpeakApp/TranscriptionProviderRegistry.swift` is the concrete Mac registry; the issue's blanket relocation claim conflates them.
3. **Mac flow:** keep concise WireUp/AppEnvironment/MainManager/HUD/delivery/history explanation backed by current code. Use SwitchingLiveTranscriber plus SharedClientLiveController/factory and list dedicated-controller exceptions only as currently present. Do not claim all cloud paths are shared until actual folds land. Include CompareModels controller/candidate/lane/fan-out as an existing shared capture consumer. Explain batch registry/OpenRouter fallback, local engines, and meaningful cancellation/finalisation ownership without asserting every provider already preserves identical metadata.
4. **iOS flow:** actual `SpeakiOSApp/SpeakiOSApp.swift`, TranscriptionRecordingService, IOSTranscriptionSession, shared state/activity/intents and foreground ownership. There is no IOSWireUp/IOSAppEnvironment at reviewed base; #1117's approved scope intentionally retains safe singletons and completes service injection. State the real OpenAI/Apple/shared/batch backend distinction. Note background/intent launch can precede foreground SwiftUI.
5. **Extensions:** table for keyboard, share, widgets, Watch and complication with source directory, imports, gate and transport/storage role. Keyboard handoff and optional direct capture must not be conflated; do not assert a numeric memory limit without a source. Share writes the existing App Group inbox, and Watch retains audio until durable import acknowledgment. Feature-gated inclusion is not proof of provisioning/distribution. Link watch-provisioning and keyboard runbooks.
6. **Persistence/sync:** separate rich Mac history/WAL from iOS snapshot/recovery unless #1118 actually changed them. Existing sync uses nine-field SyncableHistoryEntry/SyncRecord projection; local diagnostics/errorMessage/file references are not automatically synced. CloudKitKeySync has an explicit subset, not every credential. ConfigTransfer transfers selected model/keywords as string fields; SettingsSync remains a public NSUbiquitousKeyValueStore/status dependency, so avoid claiming settings exist only through transfer or that the class is dead. Read current container identifiers/zone types from ReleaseTrains.json and SpeakSync; label #1119 as an undecided consolidation issue, not a migration plan.
7. **Automation:** `Sources/SpeakApp/Transport/AutomationServer.swift`/AppAutomationHandler, SpeakAutomationKit MCPRequestHandler/MCPStdioServer, SpeakCLI and App Intents; link Docs/automation.md. Explain local socket/CLI/MCP versus iOS intents without inventing new remote control.
8. **Observability:** SentryEventScrubber, PostHogAnalytics and core ProductAnalytics dimensions/consent. State implemented privacy boundaries and distinguish instrumentation definitions from verified analytics coverage. Do not imply full runtime usage is available; #1121 schema evidence disproves that. Link analytics implementation audit/plan with their stated status.
9. **Build/release and extension points:** preserve canonical ReleaseTrains.json/generator ownership and link Alpha/Stable runbook. Current commissioning state comes from Config/ReleasePipeline.json/runbook, not stale AGENTS prose. Explicit owner-approved frozen manifest hash is required for Stable; uploads/TestFlight/merge success are distinct from public delivery. Describe actual catalogue/factory/platform registration work and options contracts; no promise that adding a provider touches one file while adapters remain.
10. **Concurrency:** name concrete MainActor managers, serial audio processors, callback locks/continuations and Mac HistoryWALStore actor. iOS persistence is currently synchronous MainActor unless a later landed patch changes it. Report actual StrictConcurrency/Swift language settings; #1122 proposed settings are not current evidence. Avoid claiming all I/O runs off MainActor or all races are solved.

## AGENTS.md and verification

Replace only its duplicated Project Structure tree/target inventory with a short relative link to Docs/Architecture.md, retaining modularisation/public API/platform guard and all operational release/build/testing rules. Do not use this docs task to rewrite instructions or resolve unrelated stale policy. Keep existing required user authorisation/Stable gates unchanged.

Check every Markdown local link and referenced source path against the actual tree, resolving document-relative links and stripping anchors correctly; the issue's grep-only .swift check misses directories/manifests and does not verify claims. Enumerate manifests again and compare exact target-name inventory with the final table, including conditional/auxiliary packages. Run a Markdown/Mermaid syntax sanity review and line count; do not add a permanent custom validator unless the repo already has a suitable docs check. Manually verify the five high-risk claims: provider routing, iOS construction, history/key/settings sync, extension capabilities and release approval.

Use git diff --check and existing docs/script checks if applicable. Documentation-only work requires no Apple build, paid provider run or device gate. PR should identify the inspected base and any explicit conditional current-state descriptions, with no target-state claims. No remote issue edits under this plan. Keep documentation current by folding actual concurrently landed path changes before integration, and submit a scoped conventional docs PR.
