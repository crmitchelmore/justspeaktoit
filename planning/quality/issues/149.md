# Issue #149 — Portable planning-memory pattern after signed-commit correction

**Status:** needs re-review
**Last updated:** 2026-03-25

## Summary
This issue now exists to define the reusable rollout contract for planning memory on repos with stricter signed-commit rules.

## Verified facts
- The corrected rollout contract now requires signed-commit compatibility to be checked before enabling repo-memory branches.
- Maintainer corrections invalidate stale labels and comments when the underlying assumption changes.
- A realistic issue is the main proof; workflow-health issues need a concrete design question.

## Quality stance
- Do **not** document Contents API as the default signed-commit-safe path.
- The smallest reusable contract for other repos is:
  1. verify Copilot-backed execution,
  2. seed per-role memory branches,
  3. verify `planning/*` writes can satisfy signed-commit rules (branch exemption or workflow signing),
  4. use a realistic proof issue,
  5. treat maintainer corrections as invalidating stale approvals.

## Approval status
Not yet. Re-approve once the corrected portable default is explicit in the thread and aligned with docs.
