# Issue #256 — test(core): error descriptions, header redactor, API validation tests

**Status:** Approved (2026-04-08)

## What it is
Pure test additions to `Tests/SpeakCoreTests/`:
- `ErrorDescriptionTests.swift` — 11 tests (value-type error string assertions)
- `SensitiveHeaderRedactorTests.swift` — 14 tests (pure function assertions)
- `APIKeyValidationResultTests.swift` — 15 tests (factory, Equatable, edge cases)

## Performance stance
No runtime code changes. No production paths touched. All tests are pure value-type assertions (no mocks, no async, no OS dependencies). Fast by construction. CI suite grows by ~40 trivially-fast tests.

## Blockers
None. Approved immediately — same pattern as #283.
