# Issue #246 — Incremental transcript append in DeepgramLiveController

**Status:** Architecture approved (2026-04-08)

## Summary
Replace O(N) `finalSegments.map(\.text).joined(separator: " ")` in `DeepgramLiveController.handleTranscript` with O(1) incremental append. `fullTranscript` is a stored property — the correct seam.

## Verification
- Line 653: Deepgram path — `finalSegments` is append-only, incremental append is safe.
- Line 1195: AssemblyAI path — requires conditional: rebuild only on the replace branch (lines 1189), append on the add branch (line 1191). Issue body correctly notes this.
- `buildResult` at lines 948/1435: one-shot at session end, not hot path — leave as-is.

## Labels at approval
- `planning:needs-quality` still present — Casey not yet approved.
- All other roles approved.

## Guardrails noted
- If PR touches AssemblyAI, conditional must guard rebuild on replace branch, not vice versa.
- `buildResult` uses `map+join` once — do not change, not hot path.
